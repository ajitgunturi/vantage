---
phase: 06-production-hardening-assignment-alignment
plan: 08
type: execute
wave: 4
depends_on: ["06-02", "06-03", "06-04", "06-05", "06-06", "06-07"]
files_modified:
  - docs/AI_PROMPTS.md
  - docs/AI_USAGE.md
  - README.md
autonomous: true
requirements: [DOC-02, DOC-03]
tags: [docs, ai-prompts, readme]

must_haves:
  truths:
    - "docs/AI_PROMPTS.md is a verbatim prompt log covering repo bootstrap, code, unit tests, and build env, with honest where-it-fell-short notes"
    - "The prompt log is linked from README and docs/AI_USAGE.md"
    - "README describes the migration Job as a post-install,post-upgrade hook (not the old pre-install semantics)"
    - "README documents the new health, probe, resource, HPA/scaling, and pagination surfaces"
  artifacts:
    - docs/AI_PROMPTS.md
  key_links:
    - "AI_PROMPTS.md failure notes trace to real incidents (C-1 ack-on-persist, M-2 missed wakeup, M-3 GracefulStop, M-4 header ordering, Phase-5 hook deadlock)"
    - "README pagination/health/HPA sections match the surfaces shipped in Plans 02-06"
---

<objective>
Write the verbatim AI-prompt log the assignment explicitly grades (DOC-02, finding F-01) and bring
the README back into accuracy — correcting the stale pre-install hook claim and documenting every
new Phase-6 surface (DOC-03, finding F-09, DOC-01 living-README cadence).

Purpose: the assignment demands "all the prompts used" plus "where a prompt fell short that required
manual intervention"; today only a summary-level AI_USAGE.md exists. And the README still describes
the migration Job with the pre-deadlock-fix hook semantics and none of the new operability surfaces.
Output: docs/AI_PROMPTS.md, links from README + AI_USAGE.md, corrected + expanded README.

## Phase Goal (MVP framing)

**As a** grader/reviewer, **I want** a reproducible prompt-by-prompt AI log with candid failure
notes and an accurate README of every shipped surface, **so that** I can verify the AI-workflow
deliverable and run every new feature from the docs alone.

Docs land last so they describe the surfaces exactly as shipped by Plans 02-07.

Note on DOC-01 cadence: to preserve wave parallelism (many Plan-06 plans run concurrently and cannot
all edit README.md), this plan carries the single consolidated README pass for the phase — satisfying
the DOC-01 per-phase living-README cadence for Phase 6.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-production-hardening-assignment-alignment/06-RESEARCH.md
@.planning/phases/06-production-hardening-assignment-alignment/06-CONTEXT.md
@docs/AI_USAGE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/AI_PROMPTS.md verbatim prompt log</name>
  <files>docs/AI_PROMPTS.md, docs/AI_USAGE.md</files>
  <read_first>
    - docs/AI_USAGE.md (existing summary-level AI-usage doc to link from and complement)
    - .planning/phases/06-production-hardening-assignment-alignment/06-RESEARCH.md (AI Prompt Log Sourcing section — table structure, stages, documented failure points)
    - .planning/quick/260702-ku8-REVIEW-FINDINGS.md and .planning/quick/260702-ku8-SUMMARY.md (C-1, M-2, M-3, M-4 incident detail)
    - .planning/phases/05-*/05-DISCUSSION-LOG.md (Phase-5 hook deadlock narrative) and docs/adr/ADR-001-bidi-at-least-once-delivery.md
  </read_first>
  <action>
    Create docs/AI_PROMPTS.md as a verbatim prompt log with a table whose columns are Stage,
    Tool/Command, Representative Prompt, Outcome, and Where-it-fell-short / manual intervention. Cover
    the assignment's four named aspects — repo bootstrap, code, unit tests, build env — organized across
    the eight stages listed in RESEARCH (bootstrap; MQ core; pipeline; gateway; devops; unit tests;
    build env / toolchain; code review + bug fix). Mine the real prompt history from the .planning
    artifacts: each GSD skill invocation (discuss-phase, plan-phase, execute-phase) and the PLAN /
    SUMMARY / DISCUSSION-LOG files are the traceable prompts. Document honest failure rows drawn verbatim
    from the sources: C-1 collector ack-on-persist data-loss bug and its smoke-reproduced fix; M-2 MQ
    missed-wakeup starvation caught by the race detector; M-3 GracefulStop hang fixed with a 5s timeout
    plus Stop() fallback; M-4 truncation-header ordering bug found in review; the Phase-5 Helm hook
    deadlock resolved by moving the migrate Job to post-install,post-upgrade; and the Phase-3 MVCC
    two-query smoke false-failure. Do not sanitize — the where-it-fell-short column is explicitly graded.
    Add a one-line cross-reference to docs/AI_PROMPTS.md inside docs/AI_USAGE.md.
  </action>
  <verify>
    <automated>bash -c 'test -f docs/AI_PROMPTS.md && grep -qi bootstrap docs/AI_PROMPTS.md && grep -qi "unit test" docs/AI_PROMPTS.md && grep -qi "build env" docs/AI_PROMPTS.md && grep -qi deadlock docs/AI_PROMPTS.md && grep -qi AI_PROMPTS docs/AI_USAGE.md && echo PROMPTS-OK'</automated>
  </verify>
  <acceptance_criteria>
    - docs/AI_PROMPTS.md exists with a table containing Stage, Tool, Prompt, Outcome, and a where-it-fell-short column.
    - It covers all four aspects: repo bootstrap, code, unit tests, build env (grep-confirmed keywords present).
    - At least the C-1, M-3 (GracefulStop), and Phase-5 hook-deadlock failure incidents appear with their manual-intervention notes.
    - docs/AI_USAGE.md links to docs/AI_PROMPTS.md.
  </acceptance_criteria>
  <done>A candid, reproducible verbatim prompt log exists covering the four aspects and real failures, linked from AI_USAGE.md.</done>
</task>

<task type="auto">
  <name>Task 2: README accuracy pass + new-surface documentation</name>
  <files>README.md</files>
  <read_first>
    - README.md lines around 405-435 (the pre-install hook description to correct) and the AI-assistance section (to link the prompt log)
    - .planning/phases/06-production-hardening-assignment-alignment/06-CONTEXT.md (surfaces to document: health, probes, resources, HPA/scaling, pagination)
    - .planning/phases/06-production-hardening-assignment-alignment/06-RESEARCH.md (README Accuracy Fixes; HPA metrics-server caveat; scaling workflow commands)
    - deployments/values.yaml and deployments/charts/gateway/templates/hpa.yaml (as shipped by Plan 06 — document actual keys)
  </read_first>
  <action>
    Correct the migration-hook description so the README states the migrate Job runs via a
    post-install,post-upgrade Helm hook (matching the 05-05 deadlock fix), replacing the current
    pre-install wording in both the ~line-411 and ~line-431 passages. Add or extend README sections
    documenting the Phase-6 surfaces exactly as shipped: (a) Health & probes — every service exposes
    /healthz + /readyz (MQ+gateway on :8080, streamer :9000, collector :9001) and Helm wires liveness/
    readiness probes; (b) Resources — per-service requests/limits defaults, overridable via values;
    (c) Scaling & HPA — gateway optional autoscaling/v2 HPA gated by gateway.autoscaling.enabled (off by
    default) with the metrics-server caveat for kind, plus the streamer/collector manual scale workflow
    (kubectl scale deployment and helm upgrade --set replica overrides, proven to 10 instances);
    (d) Pagination — GET /api/v1/gpus/{id}/telemetry supports limit + offset with a data+pagination
    envelope (limit, offset, has_next), limit ceiling = VANTAGE_GATEWAY_MAX_ROWS. Link docs/AI_PROMPTS.md
    from the AI-assistance section. Keep the living-README style consistent with existing sections.
  </action>
  <verify>
    <automated>bash -c 'grep -q "post-install,post-upgrade" README.md && grep -qi "readyz" README.md && grep -qi "offset" README.md && grep -qi "autoscaling" README.md && grep -qi AI_PROMPTS README.md && echo README-OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "post-install,post-upgrade" README.md` succeeds; the old pre-install migrate-hook wording is gone from both passages.
    - README documents /healthz + /readyz endpoints and the per-service health ports.
    - README documents limit/offset pagination with the response envelope and the limit ceiling.
    - README documents the gateway HPA toggle (with metrics-server caveat) and the streamer/collector scale workflow.
    - README's AI-assistance section links docs/AI_PROMPTS.md.
  </acceptance_criteria>
  <done>README accurately describes the post-install migrate hook and every new Phase-6 operability + pagination surface, linking the prompt log.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → public reader | Documentation must not embed secrets (DSN, tokens) while describing config |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-17 | Information Disclosure | README/AI_PROMPTS example config | medium | mitigate | Documented DSN/env examples use the local-dev vantage/vantage placeholder already in the repo; no real credentials or tokens are added (ASVS V8). |
| T-06-18 | Repudiation | AI-prompt log candor | low | accept | The where-it-fell-short column records real failures honestly; this is a deliverable requirement, not a risk to mitigate away. |
</threat_model>

<verification>
- `grep` confirms AI_PROMPTS.md covers the four aspects + failures and is linked from AI_USAGE.md and README.
- `grep` confirms README states post-install,post-upgrade and documents health/pagination/HPA/scaling.
- Manual (DOC-02 editorial, deferred to UAT): owner reads AI_PROMPTS.md for verbatim fidelity and failure-story candor.
</verification>

<success_criteria>
A candid verbatim AI-prompt log exists and is linked; the README is accurate about the migrate hook
and fully documents the new health, probe, resource, HPA/scaling, and pagination surfaces.
</success_criteria>

## Artifacts this phase produces (Plan 08)
- `docs/AI_PROMPTS.md` (verbatim prompt log — DOC-02)
- Cross-reference link in `docs/AI_USAGE.md` → AI_PROMPTS.md
- README sections: Health & probes, Resources, Scaling & HPA, Pagination; corrected migrate-hook description; AI-prompt-log link

<output>
Create `.planning/phases/06-production-hardening-assignment-alignment/06-08-SUMMARY.md` when done.
</output>
