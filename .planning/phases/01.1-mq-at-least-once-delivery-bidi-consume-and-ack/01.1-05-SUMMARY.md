---
phase: 01.1-mq-at-least-once-delivery-bidi-consume-and-ack
plan: "05"
subsystem: smoke-tooling
tags: [mqprobe, bidi, ack, credit, at-least-once, smoke, grpc-client]
status: complete

dependency_graph:
  requires:
    - 01.1-01  # bidi Consume(ctx) client + ConsumeClientMsg
    - 01.1-03  # running engine semantics (credit handshake, ack-by-id removal)
  provides:
    - Bidi at-least-once smoke probe (credit + per-message ack) for the Plan 06 smoke harness
  affects:
    - scripts/smoke/mqprobe

tech_stack:
  added: []
  patterns:
    - Bidi client handshake — Send initial ConsumeClientMsg{Credit} before the first Recv
    - Receive-and-ack loop — Send ConsumeClientMsg{AckId: msg.Id} per received message
    - CloseSend to half-close the client→server direction after the last ack

key_files:
  created: []
  modified:
    - scripts/smoke/mqprobe/main.go   # bidi consume+ack; -credit flag; drain() -> recvAndAck()/openConsume()

decisions:
  - "Factored the bidi open + credit handshake into openConsume() and the ack loop into recvAndAck(), shared by both -mode consume and -mode both — single source of the wire protocol for the probe."
  - "CloseSend() after the last ack: lets the server's recv goroutine see io.EOF and exit cleanly on the happy path (the engine also handles abrupt disconnect via ctx)."
  - "Kept the unary produce() and the run/flag scaffolding unchanged per plan; only the consume path was rewritten."

metrics:
  duration: "~1 session (inline orchestrator execution)"
  completed: "2026-06-28T00:00:00Z"
  tasks_completed: 1
  files_modified: 1
---

# Phase 01.1 Plan 05: Bidi mqprobe Summary

**One-liner:** Rewrote the `mqprobe` smoke client to speak the bidi at-least-once protocol — open `client.Consume(ctx)`, send an initial credit handshake, then receive-and-ack each message by broker id — replacing the old server-stream drain-without-ack peek and restoring `go build ./...` / `go test ./...` to green across the whole module.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Rewrite mqprobe to bidi consume + ack, add -credit flag | (this commit) | scripts/smoke/mqprobe/main.go |

## Verification Results

- `go build ./scripts/smoke/mqprobe/` — PASS
- `go vet ./scripts/smoke/mqprobe/` — PASS
- Grep gates: `ConsumeClientMsg` present, `AckId` present, `"credit"` flag present, no `ConsumeRequest` remaining — all PASS
- `go build ./...` — PASS (whole module green for the first time since the Wave 1 proto change)
- `go test -race ./...` — PASS (config, http, queue, server)

## Deviations from Plan

- **Executed inline by the orchestrator, not a gsd-executor subagent** (session-wide stream-watchdog stalls on subagent dispatch). No partial state left.
- Minor: introduced two small helpers (`openConsume`, `recvAndAck`) instead of inlining the bidi logic twice — keeps `-mode consume` and `-mode both` on one protocol implementation. Within the plan's intent (it named `recvAndAck`-style ack-on-receive).

## Threat Flags

- **T-01.1-08 (probe ack spoofing):** accepted — the probe acks only ids it actually received; it is a trusted test harness. Malicious-ack handling is enforced server-side (T-01.1-01, Plan 03).
- **T-01.1-SC:** no new packages — reuses the existing grpc client + generated pb.

## Self-Check: PASSED

- [x] `scripts/smoke/mqprobe/main.go` opens bidi `client.Consume(ctx)` and sends initial `ConsumeClientMsg{Credit}`
- [x] Receives each message and acks by `msg.GetId()` via `ConsumeClientMsg{AckId}`
- [x] `-credit` flag present (default 20), threaded into consume/runBoth
- [x] No `ConsumeRequest` reference remains; `go build` + `go vet` clean
- [x] `go build ./...` and `go test -race ./...` green
