---
phase: 01.1-mq-at-least-once-delivery-bidi-consume-and-ack
plan: "04"
subsystem: mq-http-control-plane
tags: [inspect, counters, at-least-once, observability, http, json, tdd]
status: complete

dependency_graph:
  requires:
    - 01.1-03  # ServerStats.{Delivered,Redelivered,InFlight}; Consumed = acks
  provides:
    - At-least-once counter JSON on GET /api/v1/queue/inspect (consumed by 01.1-06 smoke script)
  affects:
    - internal/http (InspectResponse extended; handler maps new fields)

tech_stack:
  added: []
  patterns:
    - 1:1 field mapping from ServerStats to InspectResponse (no logic in the handler)
    - Compact bidi acking-consumer mock to drive engine counters in an http-layer test
    - Ack-clock teardown (wait on Stats().Consumed) for deterministic counter assertions

key_files:
  created: []
  modified:
    - internal/http/inspect.go        # +DeliveredTotal/+RedeliveredTotal/+InFlight; ConsumedTotal=acks doc
    - internal/http/inspect_test.go   # +TestInspect_AtLeastOnceCounters + ackingStream mock

decisions:
  - "consumed_total semantics changed in place (now acks) rather than renamed: the smoke script and any operator dashboards keep the same key, with a doc comment recording the D-09 meaning shift."
  - "Test drives a real consumer through the engine (not a hand-set Stats) so delivered_total/consumed_total/in_flight are exercised end-to-end, then also asserts raw-JSON key presence for the smoke-script contract."

metrics:
  duration: "~1 session (inline orchestrator execution after subagent stall)"
  completed: "2026-06-28T00:00:00Z"
  tasks_completed: 1
  files_modified: 2
---

# Phase 01.1 Plan 04: At-Least-Once Inspect Counters Summary

**One-liner:** `GET /api/v1/queue/inspect` now reports the at-least-once counter set — `delivered_total` (sends), `consumed_total` (acks, D-09 meaning shift), `redelivered_total`, and an `in_flight` gauge — all mapped 1:1 from the Plan-03 `ServerStats`, making broker-side at-least-once observable over HTTP.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Extend InspectResponse with delivered/redelivered/in_flight; consumed=acks | (this commit) | inspect.go, inspect_test.go |

## Verification Results

- `go test -race -count=5 ./internal/http/` — PASS
- `go build ./internal/http/` — PASS
- `inspect.go` contains `delivered_total`, `redelivered_total`, `in_flight` JSON tags; `consumed_total` maps from `st.Consumed` (acks).
- `TestInspect_AtLeastOnceCounters` drives N=25 deliveries+acks and asserts the full 9-key JSON shape plus field mapping (`delivered_total == consumed_total == 25`, `in_flight == 0`).

## Deviations from Plan

- **Executed inline by the orchestrator, not a gsd-executor subagent.** The Plan-04 subagent dispatch stalled on the stream watchdog (600s, no progress, zero commits — same session-wide watchdog issue that stalled Plan 03). The orchestrator executed inline on the main tree; no partial state was left.

## Threat Flags

- **T-01.1-07 (info disclosure via inspect JSON):** accepted — counters are non-sensitive operational metrics; no `consumer_id`, payloads, or credentials are serialized (consumer_id is logging-only per Plan 03).
- **T-01.1-SC:** no new packages — stdlib `encoding/json` / `net/http` only.

## Self-Check: PASSED

- [x] `internal/http/inspect.go` — `DeliveredTotal`/`RedeliveredTotal`/`InFlight` fields with `delivered_total`/`redelivered_total`/`in_flight` JSON tags
- [x] Handler maps the new fields from `st.Delivered`/`st.Redelivered`/`st.InFlight`; `consumed_total` from `st.Consumed`
- [x] `internal/http/inspect_test.go` — `TestInspect_AtLeastOnceCounters` asserts the new JSON shape and passes
- [x] `go test -race ./internal/http/` PASS
