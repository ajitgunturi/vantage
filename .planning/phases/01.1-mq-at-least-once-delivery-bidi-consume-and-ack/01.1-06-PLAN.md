---
phase: 01.1-mq-at-least-once-delivery-bidi-consume-and-ack
plan: 06
type: execute
wave: 4
depends_on: ["01.1-04", "01.1-05"]
files_modified:
  - scripts/smoke/phase01-mq.sh
  - README.md
autonomous: true
requirements: [MQ-09, MQ-10, QA-06, DOC-01]
must_haves:
  truths:
    - "make smoke-01 passes end-to-end over the real gRPC bidi path: produce N, consume with credit + acks, no loss."
    - "The late-join smoke scenario proves zero loss when a consumer attaches after the producer disconnects and reads fewer than produced."
    - "The smoke inspect assertions parse the new counter names (delivered_total, consumed_total=acks, redelivered_total)."
    - "README documents bidi at-least-once delivery semantics, mqprobe -mode/-credit usage, and a current inspect JSON sample."
  artifacts:
    - scripts/smoke/phase01-mq.sh
    - README.md
  key_links:
    - "phase01-mq.sh sed parsers must match the Plan 04 inspect JSON keys (delivered_total/consumed_total/redelivered_total)."
    - "the smoke consume step must invoke the Plan 05 mqprobe bidi+ack consume so consumed_total (acks) advances."
---

<objective>
Close the phase by upgrading the manual smoke suite and the living README to the bidi at-least-once
contract (QA-06 / DOC-01 cross-cutting cadence; proves MQ-09/MQ-10 over the wire). The Phase-1
smoke script was written for at-most-once server-stream drain — it must now drive ack-based consume,
assert no loss on late-join, and parse the new inspect counters. This is the last wave because it
runs the fully-integrated stack and depends on both the new counter JSON (Plan 04) and the bidi
mqprobe (Plan 05). The ADR / instructions.md / PROJECT.md / REQUIREMENTS.md deviation artifacts are
already committed — this plan does NOT touch them; it only extends README per the DOC-01 cadence.

Purpose: prove broker-side at-least-once works across process boundaries and document it for users.
Output: rewritten `scripts/smoke/phase01-mq.sh` (ack-based + late-join no-loss + new counters) and an updated README.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01.1-mq-at-least-once-delivery-bidi-consume-and-ack/01.1-CONTEXT.md
@.planning/phases/01.1-mq-at-least-once-delivery-bidi-consume-and-ack/01.1-RESEARCH.md
@scripts/smoke/phase01-mq.sh
@README.md
</context>

<artifacts_this_phase_produces>
New/changed symbols introduced by THIS plan:
- `scripts/smoke/phase01-mq.sh` reworked: ack-based consume via mqprobe, late-join no-loss assertion, sed parsers for delivered_total/consumed_total(=acks)/redelivered_total
- README sections: bidi at-least-once delivery semantics, mqprobe -mode/-credit usage, current inspect JSON sample
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite phase01-mq.sh for ack-based consume + late-join no-loss + new counters</name>
  <read_first>
    @scripts/smoke/phase01-mq.sh
    @.planning/phases/01.1-mq-at-least-once-delivery-bidi-consume-and-ack/01.1-RESEARCH.md
    @.planning/phases/01.1-mq-at-least-once-delivery-bidi-consume-and-ack/01.1-CONTEXT.md
  </read_first>
  <files>scripts/smoke/phase01-mq.sh</files>
  <action>
    Update the smoke script (it already builds the mq, picks dedicated ports, and traps cleanup — keep that
    scaffold). For the data-plane step and the late-join step, invoke the bidi mqprobe (Plan 05) so each
    consumed message is acked; pass `-credit` if useful. Keep the late-join scenario (produce-only invocation,
    then a separate consume invocation) and strengthen it per CONTEXT "Specific Ideas": assert no loss when the
    consumer reads fewer than produced — i.e. after a consume of K < N, the remaining N-K are still retrievable
    (a follow-up consume drains them; or inspect shows depth/redelivered accounting for them). Update the inspect
    cross-check (RESEARCH Pitfall 6): parse the new counter names with sed — assert `delivered_total >= N`
    (broker sent at least N), `consumed_total >= N` (acks arrived after mqprobe acked each message), and
    `redelivered_total >= 0` (0 on the happy path; > 0 only if a disconnect-with-unacked occurred). Preserve the
    pass/fail helper style and the `✓`/`✗` output. Do not introduce jq (keep it sed-based and dependency-free).
  </action>
  <verify>
    <automated>bash -n scripts/smoke/phase01-mq.sh && grep -q 'delivered_total' scripts/smoke/phase01-mq.sh && grep -q 'redelivered_total' scripts/smoke/phase01-mq.sh && make smoke-01</automated>
  </verify>
  <acceptance_criteria>
    `make smoke-01` passes end-to-end (build → produce → bidi ack-consume → late-join no-loss → inspect counter cross-check) using the new counter names; the script parses delivered_total/consumed_total/redelivered_total; no jq dependency.
  </acceptance_criteria>
  <done>The manual smoke suite proves at-least-once over the wire, including late-join no-loss, and reads the new inspect counters.</done>
</task>

<task type="auto">
  <name>Task 2: Update README — bidi at-least-once semantics, mqprobe usage, inspect sample</name>
  <read_first>
    @README.md
    @docs/adr/ADR-001-bidi-at-least-once-delivery.md
    @.planning/phases/01.1-mq-at-least-once-delivery-bidi-consume-and-ack/01.1-CONTEXT.md
  </read_first>
  <files>README.md</files>
  <action>
    Extend the living README (DOC-01 cadence) to reflect the bidi at-least-once upgrade: (1) update the MQ
    delivery-semantics description from at-most-once server-stream to broker-side at-least-once (bidi Consume,
    client-driven credit, per-message ack by broker id, redelivery-on-disconnect, duplicates absorbed downstream),
    pointing to ADR-001; (2) document mqprobe usage including `-mode` (both|produce|consume) and the new `-credit`
    flag, noting consume now acks each message; (3) refresh the `GET /api/v1/queue/inspect` JSON sample to show the
    current counter set (produced_total, delivered_total, consumed_total as acks, redelivered_total, dropped_total,
    in_flight, active_consumers, depth, capacity). Keep the README's existing structure and quickstart; this is an
    incremental edit, not a rewrite. Do NOT edit instructions.md, PROJECT.md, or REQUIREMENTS.md — those deviation
    artifacts are already committed.
  </action>
  <verify>
    <automated>grep -qi 'at-least-once' README.md && grep -q 'credit' README.md && grep -q 'delivered_total' README.md && grep -q 'redelivered_total' README.md</automated>
  </verify>
  <acceptance_criteria>
    README describes bidi at-least-once delivery (referencing ADR-001), documents mqprobe -mode/-credit, and shows a current inspect JSON sample with delivered_total/consumed_total/redelivered_total/in_flight.
  </acceptance_criteria>
  <done>The README accurately documents the new delivery contract, probe usage, and inspect shape for a clone→run→observe reader.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → make smoke-01 | Local dev verification harness; spawns the mq on dedicated ports, no external services or secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.1-09 | Denial of Service | smoke script process lifecycle | low | mitigate | Existing trap-based cleanup kills the mq PID and removes the temp dir on EXIT; dedicated ports avoid clashing with a dev instance. |
| T-01.1-SC | Tampering | dependencies | low | accept | No new packages; sed/curl/go only — dependency-free shell, no jq. |
</threat_model>

<verification>
- `bash -n scripts/smoke/phase01-mq.sh` parses clean; `make smoke-01` passes.
- README contains at-least-once semantics, -credit usage, and the new inspect counter sample.
</verification>

<success_criteria>
The phase is provable end-to-end: `make smoke-01` demonstrates bidi at-least-once with no loss on late-join and the new inspect counters, and the README documents the contract for users.
</success_criteria>

<output>
Create `.planning/phases/01.1-mq-at-least-once-delivery-bidi-consume-and-ack/01.1-06-SUMMARY.md` when done.
</output>
