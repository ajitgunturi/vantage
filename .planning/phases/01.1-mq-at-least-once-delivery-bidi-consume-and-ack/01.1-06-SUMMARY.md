---
phase: 01.1-mq-at-least-once-delivery-bidi-consume-and-ack
plan: "06"
subsystem: smoke-suite, docs
tags: [smoke, at-least-once, late-join, no-loss, inspect, readme, doc-01, qa-06]
status: complete

dependency_graph:
  requires:
    - 01.1-04  # inspect counter JSON (delivered_total/consumed_total=acks/redelivered_total)
    - 01.1-05  # bidi mqprobe (credit + per-message ack)
  provides:
    - End-to-end over-the-wire proof of bidi at-least-once + late-join no-loss (make smoke-01)
    - README documentation of the delivery contract, mqprobe usage, inspect shape
  affects:
    - scripts/smoke (phase01-mq.sh)
    - README.md

tech_stack:
  added: []
  patterns:
    - jq-free sed counter parsing of the inspect JSON
    - Three-process late-join no-loss scenario (produce N / consume HALF / drain REST)
    - trap-based mq lifecycle on dedicated ports (unchanged scaffold)

key_files:
  created: []
  modified:
    - scripts/smoke/phase01-mq.sh   # bidi ack consume; late-join no-loss; new counter cross-check
    - README.md                     # at-least-once semantics, mqprobe -credit/ack, inspect sample

decisions:
  - "Late-join no-loss is proven by a third process draining the remainder, not by inspecting depth alone: a successful drain of the other N-K messages IS the retention proof, and it also drives redelivered_total positive (the partial consumer disconnects holding unacked leases)."
  - "Counter assertions use >= N (not exact) because the script produces across multiple steps; the exact identity (delivered=consumed+redelivered+in_flight) is documented in the README rather than asserted, to keep the smoke check robust."

metrics:
  duration: "~1 session (inline orchestrator execution)"
  completed: "2026-06-28T00:00:00Z"
  tasks_completed: 2
  files_modified: 2
---

# Phase 01.1 Plan 06: Smoke Suite + README At-Least-Once Summary

**One-liner:** `make smoke-01` now proves broker-side at-least-once end-to-end over the real gRPC wire — bidi credit+ack consume, a late-join no-loss scenario (read 10/20, drain the remaining 10), and a sed cross-check of the new inspect counters where `redelivered_total` goes positive exactly when the partial consumer disconnects holding unacked leases — and the README documents the delivery contract, `mqprobe -credit`, and the new inspect JSON.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Rewrite phase01-mq.sh for ack-based consume + late-join no-loss + new counters | (commit 1) | scripts/smoke/phase01-mq.sh |
| 2 | Update README — bidi at-least-once semantics, mqprobe usage, inspect sample | (commit 2) | README.md |

## Verification Results

- `bash -n scripts/smoke/phase01-mq.sh` — clean
- Smoke grep gates: `delivered_total`, `redelivered_total` present — PASS
- `make smoke-01` — **PASS** end-to-end. Live counters: `produced_total=40 delivered_total=46 consumed_total(acks)=40 redelivered_total=6 in_flight=0 depth=0` — zero loss, and `delivered = consumed + redelivered` (46 = 40 + 6) holds. The redelivered=6 proves redelivery-on-disconnect over the wire (the credit-8 partial consumer left 6 unacked, requeued and drained by the next consumer).
- README grep gates: `at-least-once`, `credit`, `delivered_total`, `redelivered_total` present — PASS
- `go build ./...` + `go test ./...` — PASS

## Deviations from Plan

- **Executed inline by the orchestrator, not a gsd-executor subagent** (session-wide stream-watchdog stalls). No partial state left.
- Did NOT touch instructions.md / PROJECT.md / REQUIREMENTS.md / ADR-001 (already-committed deviation artifacts), per the plan.

## Threat Flags

- **T-01.1-09 (smoke lifecycle DoS):** mitigated — the existing trap-based cleanup kills the mq PID and removes the temp dir on EXIT; dedicated ports (:55051/:58080) avoid clashing with a dev instance.
- **T-01.1-SC:** no new packages — sed/curl/go only; jq-free.

## Self-Check: PASSED

- [x] `scripts/smoke/phase01-mq.sh` drives bidi ack-consume and a late-join no-loss scenario (produce N / consume HALF / drain REST)
- [x] Script parses `delivered_total`, `consumed_total` (acks), `redelivered_total` with sed (no jq)
- [x] `make smoke-01` passes end-to-end
- [x] README documents bidi at-least-once (ADR-001), `mqprobe -mode/-credit` with ack-on-receive, and the current inspect JSON sample
- [x] Both task commits exist
