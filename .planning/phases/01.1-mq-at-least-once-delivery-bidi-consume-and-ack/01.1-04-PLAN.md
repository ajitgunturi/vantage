---
phase: 01.1-mq-at-least-once-delivery-bidi-consume-and-ack
plan: 04
type: execute
wave: 3
depends_on: ["01.1-03"]
files_modified:
  - internal/http/inspect.go
  - internal/http/inspect_test.go
autonomous: true
requirements: [MQ-09]
must_haves:
  truths:
    - "GET /api/v1/queue/inspect reports consumed_total as the count of acks (confirmed deliveries), not sends."
    - "The inspect JSON includes delivered_total (messages sent) and redelivered_total (re-enqueued on disconnect)."
    - "The inspect JSON includes an in_flight gauge (global sum of per-consumer leases)."
  artifacts:
    - internal/http/inspect.go
    - internal/http/inspect_test.go
  key_links:
    - "InspectResponse fields map 1:1 from ServerStats (Delivered/Consumed/Redelivered/InFlight) produced by Plan 03."
---

<objective>
Expose the new at-least-once counters on the control plane (D-09 / MQ-09). After the engine
rewrite, `consumed_total` means acks (confirmed deliveries) rather than sends; surface the new
`delivered_total`, `redelivered_total`, and `in_flight` fields so operators (and the smoke
script in Plan 06) can observe at-least-once behavior over HTTP.

Purpose: make broker-side at-least-once observable via `GET /api/v1/queue/inspect`.
Output: extended `InspectResponse` + handler wiring + a test asserting the new JSON shape.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01.1-mq-at-least-once-delivery-bidi-consume-and-ack/01.1-CONTEXT.md
@.planning/phases/01.1-mq-at-least-once-delivery-bidi-consume-and-ack/01.1-PATTERNS.md
@internal/http/inspect.go
</context>

<artifacts_this_phase_produces>
New/changed symbols introduced by THIS plan:
- `InspectResponse` fields: `DeliveredTotal int64` (`delivered_total`), `RedeliveredTotal int64` (`redelivered_total`), `InFlight int64` (`in_flight`); `ConsumedTotal` semantics change to acks
- new/extended test in `internal/http/inspect_test.go` asserting the new JSON keys
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend InspectResponse with delivered/redelivered/in_flight, change consumed semantics</name>
  <read_first>
    @internal/http/inspect.go
    @.planning/phases/01.1-mq-at-least-once-delivery-bidi-consume-and-ack/01.1-PATTERNS.md
  </read_first>
  <files>internal/http/inspect.go, internal/http/inspect_test.go</files>
  <behavior>
    - The inspect JSON contains keys: capacity, depth, produced_total, delivered_total, consumed_total, redelivered_total, dropped_total, active_consumers, in_flight.
    - consumed_total is sourced from ServerStats.Consumed (now acks); delivered_total from Delivered; redelivered_total from Redelivered; in_flight from InFlight.
    - A request against a server that has delivered D, acked A, redelivered R messages reports delivered_total=D, consumed_total=A, redelivered_total=R.
  </behavior>
  <action>
    Extend `InspectResponse` in inspect.go per the PATTERNS struct map: add `DeliveredTotal int64 \`json:"delivered_total"\``,
    `RedeliveredTotal int64 \`json:"redelivered_total"\``, and `InFlight int64 \`json:"in_flight"\``; keep
    `ConsumedTotal` (now fed by acks) and update its doc comment to say it counts acks (D-09). In
    `InspectHandler`, map the new fields from `st.Delivered`, `st.Redelivered`, `st.InFlight` (ServerStats from
    Plan 03), preserving the existing Content-Type + WriteHeader + json.NewEncoder pattern. Add or extend
    `inspect_test.go` to drive an MQServer through some deliveries+acks (via the same bidi mock pattern, or by
    asserting the JSON keys exist and map from a Stats snapshot) and assert the response body contains
    delivered_total, consumed_total, redelivered_total, and in_flight with the expected values.
  </action>
  <verify>
    <automated>go test ./internal/http/ && go build ./internal/http/ && grep -q 'delivered_total' internal/http/inspect.go && grep -q 'redelivered_total' internal/http/inspect.go && grep -q 'in_flight' internal/http/inspect.go</automated>
  </verify>
  <acceptance_criteria>
    `InspectResponse` exposes delivered_total, redelivered_total, and in_flight; consumed_total maps from acks; the inspect test asserts the new JSON shape and passes.
  </acceptance_criteria>
  <done>The control plane reports the at-least-once counters; the new JSON contract is test-locked for the smoke script.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → GET /api/v1/queue/inspect | Read-only control-plane endpoint; exposes counters only, no mutation, no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.1-07 | Information Disclosure | inspect JSON | low | accept | Counters are non-sensitive operational metrics; no consumer_id, payloads, or credentials are exposed. consumer_id is logging-only and never serialized into inspect (Plan 03). |
| T-01.1-SC | Tampering | dependencies | low | accept | No new packages; stdlib `encoding/json`/`net/http` only. |
</threat_model>

<verification>
- `go test ./internal/http/` passes.
- inspect.go contains delivered_total, redelivered_total, in_flight JSON tags; consumed_total maps from acks.
</verification>

<success_criteria>
`GET /api/v1/queue/inspect` returns the at-least-once counter set (consumed_total=acks, delivered_total, redelivered_total, in_flight), test-verified.
</success_criteria>

<output>
Create `.planning/phases/01.1-mq-at-least-once-delivery-bidi-consume-and-ack/01.1-04-SUMMARY.md` when done.
</output>
