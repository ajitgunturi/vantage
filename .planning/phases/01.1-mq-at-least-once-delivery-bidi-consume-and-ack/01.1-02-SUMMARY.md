---
phase: 01.1-mq-at-least-once-delivery-bidi-consume-and-ack
plan: "02"
subsystem: queue-storage, config
tags: [requeue, at-least-once, ring-buffer, config, consume-credit, tdd]
status: complete

dependency_graph:
  requires:
    - 01.1-01  # proto bidi Consume + ConsumeClientMsg (pkg/pb already generated)
  provides:
    - Store.Requeue front-insertion for disconnect redelivery (consumed by 01.1-03 engine)
    - Config.ConsumeCredit default credit window (consumed by 01.1-03 engine)
  affects:
    - internal/queue (Store interface extended)
    - internal/config (Config struct extended, WorkChCap deprecated)

tech_stack:
  added: []
  patterns:
    - TDD RED/GREEN cycle for Requeue (failing test → implementation → all-green)
    - Reverse-iterate-and-front-insert pattern for ordered requeue in a ring buffer
    - Drop-newest (tail eviction) during Requeue to prioritize redelivery over new messages
    - env guard pattern: strconv.Atoi + n > 0 rejects garbage without crashing

key_files:
  created: []
  modified:
    - internal/queue/store.go         # Store.Requeue added to interface
    - internal/queue/ring_store.go    # RingStore.Requeue implementation
    - internal/queue/ring_store_test.go  # TestRingStore_Requeue + concurrent variant
    - internal/config/config.go       # ConsumeCredit field + MQ_CONSUME_CREDIT parse; WorkChCap deprecated
    - internal/config/config_test.go  # three new ConsumeCredit test cases

decisions:
  - "Drop-newest (tail eviction) during Requeue: prioritizes in-flight redelivery over newly buffered messages; mirrors RESEARCH Pattern 3 / PATTERNS recommendation."
  - "Reverse-iterate msgs during Requeue so msgs[0] lands at head: single-pass, no auxiliary allocation, O(n) per call."
  - "WorkChCap retained but deprecated with doc comment: zero-change callers until cmd/mq rewire in Plan 03 removes the workCh entirely."
  - "ConsumeCredit env guard rejects n <= 0 (matches MQ_BUFFER_SIZE guard pattern for consistency and T-01.1-03 mitigation)."

metrics:
  duration: "~4 minutes"
  completed: "2026-06-28T10:21:00Z"
  tasks_completed: 2
  files_modified: 5
---

# Phase 01.1 Plan 02: Requeue Storage + ConsumeCredit Config Summary

**One-liner:** Front-inserting `Store.Requeue` with drop-newest eviction and `Config.ConsumeCredit` env knob (default 20) — the two stateless foundations the Wave 2 bidi engine needs before it can redeliver unacked leases on disconnect.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Add Requeue to Store interface + RingStore front-insertion (TDD) | 57db473 | store.go, ring_store.go, ring_store_test.go |
| 2 | Add ConsumeCredit config + MQ_CONSUME_CREDIT env, deprecate WorkChCap | d988722 | config.go, config_test.go |

## Verification Results

- `go test -race -run TestRingStore_Requeue ./internal/queue/` — PASS
- `go test -race -count=1 ./internal/queue/` — PASS (all existing tests unaffected)
- `go test ./internal/config/` — PASS (100% coverage)
- `go build ./internal/queue/ ./internal/config/` — PASS
- Pre-existing build failures in `cmd/mq`, `internal/server`, `scripts/smoke/mqprobe` — documented in 01.1-01-SUMMARY, out of scope for this plan.

## Deviations from Plan

None — plan executed exactly as written. TDD RED/GREEN cycle followed per task 1 (`tdd="true"`): failing test committed first, implementation brought it green, no refactor needed.

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or schema changes introduced. Threat mitigations applied inline:

- **T-01.1-03 (DoS via MQ_CONSUME_CREDIT):** `strconv.Atoi + n > 0` guard rejects negative/zero/garbage; default 20 stands.
- **T-01.1-04 (DoS via full-ring Requeue):** Requeue is bounded by ring capacity; full-ring eviction drops the newest tail entry (counted in `dropped`) — no unbounded growth.
- **T-01.1-SC:** No new packages; stdlib `sync`/`strconv`/`os` only.

## Self-Check: PASSED

- [x] `internal/queue/store.go` — `Requeue` method present in Store interface
- [x] `internal/queue/ring_store.go` — `RingStore.Requeue` implemented; `var _ Store = (*RingStore)(nil)` assertion holds
- [x] `internal/queue/ring_store_test.go` — `TestRingStore_Requeue` and `TestRingStore_Requeue_Concurrent` present
- [x] `internal/config/config.go` — `ConsumeCredit int` field present, default 20, WorkChCap marked deprecated
- [x] `internal/config/config_test.go` — three new ConsumeCredit test cases present
- [x] Commit 57db473 exists (Task 1)
- [x] Commit d988722 exists (Task 2)
