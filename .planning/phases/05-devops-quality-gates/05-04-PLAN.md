---
phase: 05-devops-quality-gates
plan: 04
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - scripts/smoke/phase05-kind.sh
  - scripts/soak.sh
  - README.md
autonomous: true
requirements: [OPS-03]
must_haves:
  truths:
    - "make smoke-05 proves full E2E through kind: all pods Running → migration Job complete → streamer publishing → port-forwarded gateway returns real rows from /api/v1/gpus and /telemetry (D-12)"
    - "smoke-05 fails fast with a clear message if the kind cluster / deploy is absent (D-13)"
    - "smoke-05 demonstrates OPS-03 independent deploy — helm upgrade --reuse-values --set mq.image.tag=dev rolls only the MQ Deployment (D-02)"
    - "make soak sustains the pipeline for a configurable duration with a configurable streamer replica count, asserting row counts grow and MQ inspect shows no runaway depth / loss (D-14)"
    - "README documents the Phase-5 deploy/smoke/soak/test-harness workflow (DOC-01 cadence)"
  artifacts:
    - scripts/smoke/phase05-kind.sh
    - scripts/soak.sh
    - README.md
  key_links:
    - "smoke-05 is picked up by the existing Makefile smoke-% rule (scripts/smoke/phase05-*.sh)"
    - "soak.sh is invoked by the make soak target created in 05-01"
    - "port-forward uses local 8081→gateway 8080 (avoids MQ 8080 clash per Pitfall 8)"
---

<objective>
Add the human-run kind E2E smoke check and the soak target that prove the deployed pipeline works and sustains load, plus the Phase-5 README section. This closes the operator's demo + endurance workflow on top of the deployed chart (05-02) using the targets wired in 05-01.

Purpose: OPS-03 (independent deploy demonstrated end-to-end in kind) plus the DOC-01/QA-06 living-README + smoke cadence, and the D-14 soak target.
Output: scripts/smoke/phase05-kind.sh, scripts/soak.sh, updated README.md.
</objective>

## Phase Goal

**As a** platform operator, **I want to** build, deploy, and validate the entire vantage pipeline on a local Kubernetes cluster with single make commands, **so that** I can run and prove the end-to-end system reproducibly.

This plan delivers the operator's demo + endurance capability: `make smoke-05` proves the deployed pipeline serves real rows and rolls services independently; `make soak` proves sustained throughput.

## Artifacts this phase produces

New symbols/files introduced by THIS plan:
- `scripts/smoke/phase05-kind.sh` (kind E2E smoke, run via `make smoke-05`)
- `scripts/soak.sh` (sustained-load soak, run via `make soak`; env SOAK_DURATION, SOAK_STREAMERS)
- README.md Phase-5 section (deploy / smoke-05 / soak / test-harness workflow)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-devops-quality-gates/05-CONTEXT.md
@.planning/phases/05-devops-quality-gates/05-RESEARCH.md
@.planning/phases/05-devops-quality-gates/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/smoke/phase05-kind.sh — full kind E2E smoke + OPS-03 independence demo</name>
  <files>scripts/smoke/phase05-kind.sh</files>
  <read_first>
    - scripts/smoke/phase04-gateway.sh (header style, color helpers, pass/fail functions, curl assertion pattern to copy)
    - .planning/phases/05-devops-quality-gates/05-PATTERNS.md (scripts/smoke/phase05-kind.sh section — header, cluster-guard, port-forward, curl assertion patterns)
    - .planning/phases/05-devops-quality-gates/05-RESEARCH.md (D-12/D-13 smoke scope; Pitfall 8 port-forward conflict; System Architecture Diagram smoke-05 steps)
    - Makefile (smoke-% rule lines 79-82 — file must be named scripts/smoke/phase05-*.sh to be discovered by make smoke-05)
  </read_first>
  <action>
    Create scripts/smoke/phase05-kind.sh following the phase04-gateway.sh style (set -euo pipefail, ROOT/cd, color helpers, pass()/fail() functions). It assumes the cluster + deploy already exist (D-13): first guard with kubectl get nodes (fail fast with "kind cluster not running — run: make kind-up deploy" if absent). Then assert deployment health: the migration Job completed (kubectl wait --for=condition=complete job/vantage-migrate or check succeeded) and all service pods reach Running (kubectl wait --for=condition=available deployment for mq/collector/gateway; streamer may be a bare Deployment). Port-forward the gateway to LOCAL port 8081 (kubectl port-forward svc/vantage-gateway 8081:8080) — use 8081 not 8080 to avoid the MQ HTTP 8080 clash (Pitfall 8); trap-cleanup the PID on EXIT. Assert GET http://localhost:8081/api/v1/gpus returns 200 with a non-empty JSON array (streamer must have published and collector persisted), then GET that GPU's /api/v1/gpus/{id}/telemetry returns 200 with rows (D-12).
    Add the OPS-03 independence demonstration: run helm upgrade --reuse-values --set mq.image.tag=dev vantage deployments and assert (via kubectl rollout status / observed generation) that ONLY the mq Deployment rolls while streamer/collector/gateway Deployments are untouched (D-02). Keep the script idempotent and re-runnable. Use pass/fail messages consistent with prior phase smoke scripts.
  </action>
  <verify>
    <automated>bash -n scripts/smoke/phase05-kind.sh && grep -q '8081:8080' scripts/smoke/phase05-kind.sh && grep -q 'reuse-values' scripts/smoke/phase05-kind.sh</automated>
  </verify>
  <acceptance_criteria>
    - scripts/smoke/phase05-kind.sh passes `bash -n` (syntax valid) and starts with set -euo pipefail
    - Script guards on kubectl get nodes and fails with a clear "run: make kind-up deploy" message when the cluster is absent (D-13)
    - Port-forward binds gateway to local 8081 (grep shows 8081:8080), with a trap cleanup killing the PID on EXIT
    - Script asserts GET /api/v1/gpus → 200 non-empty and a telemetry fetch → 200 (D-12)
    - Script runs `helm upgrade --reuse-values --set mq.image.tag=dev` and checks only the mq Deployment rolls (OPS-03, D-02)
    - File name matches scripts/smoke/phase05-*.sh so `make smoke-05` discovers it
  </acceptance_criteria>
  <done>make smoke-05 (against a deployed kind cluster) proves the full pipeline serves real rows and demonstrates independent MQ rollout; fails fast without a cluster.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/soak.sh (sustained load) + README Phase-5 workflow section</name>
  <files>scripts/soak.sh, README.md</files>
  <read_first>
    - scripts/smoke/phase05-kind.sh (created in Task 1 — reuse its cluster-guard, color, pass/fail conventions)
    - .planning/phases/05-devops-quality-gates/05-RESEARCH.md (D-14 soak scope; System Architecture Diagram soak steps — kubectl scale streamer, poll MQ inspect depth + Postgres row count growth)
    - .planning/phases/05-devops-quality-gates/05-CONTEXT.md (D-14 configurable duration/replica count; Claude's Discretion — exact soak defaults + env override names)
    - README.md (current structure + the existing per-phase "smoke" / quickstart sections to extend, not replace — DOC-01 cadence)
  </read_first>
  <action>
    Create scripts/soak.sh (invoked by the make soak target from 05-01) following the smoke script conventions (set -euo pipefail, cluster guard, pass/fail). Read two env overrides with defaults (Claude's Discretion): SOAK_DURATION (default 60, seconds) and SOAK_STREAMERS (default 3). Scale the streamer Deployment via kubectl scale deployment/vantage-streamer --replicas=$SOAK_STREAMERS (proves the up-to-10-concurrent-streamer requirement without bloating smoke). Sample the Postgres gpu_metrics row count at start and end (via kubectl exec into the postgres pod running psql, or a port-forward + psql) and assert it grew. Poll the MQ inspect endpoint (port-forward vantage-mq HTTP to a local port, e.g. 8082, and curl /api/v1/queue/inspect) across the duration and assert no runaway queue depth and no loss (produced >= acked, depth bounded) per D-14/D-19-style reconciliation. On exit, scale streamer back to 1 and clean up port-forwards (trap). Keep it re-runnable and hermetic w.r.t. its own port-forwards.
    Update README.md: extend the living README with a Phase-5 section documenting the operator workflow — make kind-up, make deploy (docker → kind-load → helm-install with the pre-install migration hook), make smoke-05, make soak (with SOAK_DURATION / SOAK_STREAMERS), and make test-harness. Note the environment prerequisites (kind at ~/go/bin, Rancher DOCKER_HOST, IfNotPresent + kind-load caveat that a forgotten kind-load surfaces as ImagePullBackOff). Do NOT rewrite existing README sections — append/extend per the DOC-01 incremental cadence.
  </action>
  <verify>
    <automated>bash -n scripts/soak.sh && grep -q 'SOAK_STREAMERS' scripts/soak.sh && grep -q 'kubectl scale' scripts/soak.sh && grep -qi 'smoke-05\|make deploy' README.md</automated>
  </verify>
  <acceptance_criteria>
    - scripts/soak.sh passes `bash -n` and starts with set -euo pipefail
    - soak.sh reads SOAK_DURATION (default 60) and SOAK_STREAMERS (default 3) env overrides
    - soak.sh scales the streamer Deployment (`kubectl scale deployment/vantage-streamer --replicas=$SOAK_STREAMERS`), samples Postgres row count growth, and polls MQ /api/v1/queue/inspect for bounded depth / no loss (D-14)
    - soak.sh restores streamer replicas and cleans up port-forwards on exit (trap)
    - README.md contains a Phase-5 workflow section referencing make deploy, make smoke-05, make soak, and make test-harness, plus the kind/Rancher env prerequisites
    - Existing README sections are preserved (extended, not replaced)
  </acceptance_criteria>
  <done>make soak sustains configurable load and asserts growth + no loss; README documents the full Phase-5 operator workflow incrementally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell → kind cluster | smoke/soak drive kubectl (port-forward, scale, exec) against the cluster |
| curl client → port-forwarded services | assertions over localhost 8081 (gateway) / 8082 (MQ inspect) |

## STRIDE Threat Register (ASVS L1, blocking threshold: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | Information Disclosure | psql invocation in soak | low | accept | DSN/credentials are LOCAL DEV only (vantage/vantage); soak runs against the local kind cluster, credentials never logged in assertion output |
| T-05-09 | Denial of Service | leaked port-forward / scaled streamers | low | mitigate | trap cleanup kills port-forwards and scales streamer back to 1 on exit; scripts are re-runnable and idempotent |
| T-05-SC | Tampering | shell scripts invoking cluster tooling | low | accept | Scripts call only pinned local tooling (kubectl/helm/curl); no package-manager installs introduced in this plan |
</threat_model>

<verification>
- `bash -n scripts/smoke/phase05-kind.sh` and `bash -n scripts/soak.sh` pass
- `make smoke-05` (against a deployed kind cluster) proves E2E + independent MQ rollout
- `make soak` sustains load and asserts row growth + bounded MQ depth
- README has the Phase-5 workflow section
</verification>

<success_criteria>
make smoke-05 proves the deployed pipeline serves real rows and rolls MQ independently (OPS-03); make soak sustains configurable load and asserts no loss; README documents the operator workflow (DOC-01).
</success_criteria>

<output>
Create `.planning/phases/05-devops-quality-gates/05-04-SUMMARY.md` when done
</output>
