---
phase: 05-devops-quality-gates
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - docker-compose.full.yml
  - testdata/fixture.csv
  - test/harness/harness_test.go
  - go.mod
  - go.sum
autonomous: true
requirements: [QA-01, QA-04]
must_haves:
  truths:
    - "make test-harness spins the full five-image + Postgres stack via docker-compose.full.yml, asserts end-to-end pipeline correctness against live containers, and tears the stack down even on failure (D-15..D-19)"
    - "D-16: default target is a docker-compose full stack (all five images + Postgres, extending docker-compose.yml); the same suite is pointable at a kind/Helm deployment via env-provided base URLs (skipping stack startup)"
    - "The harness lives in test/harness/ behind a //go:build e2e tag so make test (go test ./...) and the coverage gate both exclude it (QA-04 not diluted)"
    - "make test passes across all internal/ and pkg/ packages (QA-01) and make coverage still enforces >= 90% line coverage (QA-04)"
    - "KEEP=1 leaves the stack running for debugging; default run is hermetic up→test→down"
  artifacts:
    - docker-compose.full.yml
    - testdata/fixture.csv
    - test/harness/harness_test.go
    - go.mod
  key_links:
    - "harness imports github.com/testcontainers/testcontainers-go/modules/compose@v0.43.0 (same version as testcontainers-go core)"
    - "docker-compose.full.yml references vantage/<svc>:dev images built by make docker (05-01) and mounts testdata/fixture.csv into the streamer"
    - "coverage gate command (go list ./internal/... ./pkg/...) naturally excludes test/harness/"
---

<objective>
Add the live-infrastructure E2E test harness (user-requested first-class deliverable, D-15) and close the QA-01/QA-04 quality gates. `make test-harness` (target created in 05-01) programmatically owns a full docker-compose stack of all five images plus Postgres, runs pipeline-correctness assertions across real container boundaries, and tears down hermetically. The new harness package must not dilute the ≥90% coverage gate.

Purpose: QA-01 (unit tests across all services confirmed green) and QA-04 (≥90% coverage gate holds with the new e2e package excluded), plus the D-15..D-19 test harness.
Output: docker-compose.full.yml, testdata/fixture.csv, test/harness/harness_test.go, go.mod/go.sum (compose module added).
</objective>

## Phase Goal

**As a** platform operator, **I want to** build, deploy, and validate the entire vantage pipeline on a local Kubernetes cluster with single make commands, **so that** I can run and prove the end-to-end system reproducibly.

This plan delivers the operator's programmatic validation capability: `make test-harness` proves the pipeline against live containers, and the quality gate stays green.

## Artifacts this phase produces

New symbols/files introduced by THIS plan:
- `docker-compose.full.yml` (five services + Postgres, extends docker-compose.yml)
- `testdata/fixture.csv` (DCGM 12-column fixture for the streamer volume mount)
- `test/harness/harness_test.go` (package harness_test, build tag `e2e`, `TestHarness`)
- go.mod dependency: `github.com/testcontainers/testcontainers-go/modules/compose@v0.43.0`

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-devops-quality-gates/05-CONTEXT.md
@.planning/phases/05-devops-quality-gates/05-RESEARCH.md
@.planning/phases/05-devops-quality-gates/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: docker-compose.full.yml + testdata/fixture.csv (full five-image stack)</name>
  <files>docker-compose.full.yml, testdata/fixture.csv</files>
  <read_first>
    - docker-compose.yml (Postgres service + healthcheck to copy verbatim; credential convention vantage/vantage)
    - .planning/phases/05-devops-quality-gates/05-RESEARCH.md (docker-compose.full.yml Structure; Complete Env Variable Map; port-conflict note gateway 8090:8080)
    - .planning/phases/05-devops-quality-gates/05-PATTERNS.md (docker-compose.full.yml section — env var source, port rules, streamer CSV volume)
    - test/e2e/pipeline_test.go (writeFixtureCSV around lines 186-205 — exact DCGM 12-column header + row layout to reproduce in testdata/fixture.csv)
    - internal/streamer/config.go, internal/collector/config.go, internal/config/config.go, internal/gateway/config.go (authoritative env var names)
  </read_first>
  <action>
    Create docker-compose.full.yml at the repo root extending the existing docker-compose.yml topology. Include a postgres service identical to docker-compose.yml (postgres:17-alpine, POSTGRES_DB/USER/PASSWORD=vantage, healthcheck pg_isready). Add migrate (image vantage/migrate:dev, depends_on postgres condition service_healthy, VANTAGE_DB_DSN=postgres://vantage:vantage@postgres:5432/vantage?sslmode=disable, restart: "no" — runs once and exits 0). Add mq (image vantage/mq:dev, MQ_GRPC_ADDR=":50051", MQ_HTTP_ADDR=":8080", ports 50051:50051 and 8080:8080). Add streamer (image vantage/streamer:dev, depends_on mq, STREAMER_MQ_ADDR="mq:50051", STREAMER_CSV_PATH="/data/dcgm_metrics.csv", STREAMER_LOOP_DELAY_MS="5", volume ./testdata/fixture.csv:/data/dcgm_metrics.csv:ro). Add collector (image vantage/collector:dev, depends_on mq + postgres, COLLECTOR_MQ_ADDR="mq:50051", VANTAGE_DB_DSN=postgres://vantage:vantage@postgres:5432/vantage?sslmode=disable). Add gateway (image vantage/gateway:dev, depends_on postgres, GATEWAY_ADDR=":8080", same DSN, ports 8090:8080 — mapped to 8090 to avoid clashing with mq's 8080 per RESEARCH port-conflict note).
    Create testdata/fixture.csv as a small (10–50 row) DCGM 12-column CSV: header Timestamp,metric_name,gpu_id,device,uuid,model_name,hostname,container,pod,namespace,value,labels_raw followed by rows with distinct (uuid, metric_name) pairs (structure per test/e2e/pipeline_test.go writeFixtureCSV) so exactly-once holds. Use env var names exactly as read from the config source files.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.full.yml config >/dev/null && head -1 testdata/fixture.csv | grep -q 'Timestamp,metric_name,gpu_id'</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f docker-compose.full.yml config` exits 0 (valid compose file)
    - docker-compose.full.yml defines services: postgres, migrate, mq, streamer, collector, gateway
    - gateway maps host port 8090 to container 8080 (avoids mq 8080 clash); mq maps 50051 and 8080
    - streamer mounts ./testdata/fixture.csv at /data/dcgm_metrics.csv:ro and sets STREAMER_CSV_PATH=/data/dcgm_metrics.csv
    - migrate and collector/gateway set VANTAGE_DB_DSN to postgres://vantage:vantage@postgres:5432/vantage?sslmode=disable
    - testdata/fixture.csv first line is the DCGM 12-column header and the file has at least 10 data rows with distinct (uuid,metric_name) pairs
  </acceptance_criteria>
  <done>A valid full-stack compose file references all five vantage/*:dev images with correct env/ports and a mounted fixture CSV.</done>
</task>

<task type="auto">
  <name>Task 2: test/harness/harness_test.go (e2e-tagged) + compose module dependency + coverage-gate closure</name>
  <files>test/harness/harness_test.go, go.mod, go.sum</files>
  <read_first>
    - .planning/phases/05-devops-quality-gates/05-RESEARCH.md (Pattern 7 testcontainers compose; Coverage Gate table; Pitfall 6 coverage dilution; D-17/D-18/D-19)
    - .planning/phases/05-devops-quality-gates/05-PATTERNS.md (test/harness/harness_test.go section — build tag, imports, stack lifecycle, KEEP=1, poll helper, assertion style)
    - test/e2e/pipeline_test.go (build-tag isolation on line 1; pollUntilStable poll pattern lines ~211-240; require assertion style)
    - Makefile (test-harness target added in 05-01; coverage target lines 62-69 — package list go list ./internal/... ./pkg/... excludes test/harness naturally)
  </read_first>
  <action>
    Add the compose module: run go get github.com/testcontainers/testcontainers-go/modules/compose@v0.43.0 (same version as testcontainers-go core already in go.mod — pinned per RESEARCH Package Legitimacy Audit, verdict OK) then go mod tidy so go.mod/go.sum record it.
    Create test/harness/harness_test.go with //go:build e2e as the FIRST line (separate tag from the existing integration suite so it runs only via make test-harness and is excluded from go test ./... and the coverage gate — Pitfall 6). package harness_test. In TestHarness: build the stack with compose.NewDockerComposeWith(compose.WithStackFiles("../../docker-compose.full.yml"), compose.StackIdentifier("vantage-harness")); register a t.Cleanup that returns early if os.Getenv("KEEP")=="1" (leave running) else calls stack.Down(ctx, compose.RemoveOrphans(true), compose.RemoveVolumes(true)) — teardown runs even on failure (D-18). Up the stack waiting for the gateway service to listen on 8080/tcp (compose.Wait(true)). Then assert pipeline correctness across real container boundaries (D-19): poll GET http://localhost:8090/api/v1/gpus (gateway host port 8090) until it returns 200 with a non-empty JSON array within a ~30–60s deadline (adapt the pollUntilStable retry pattern to HTTP), assert at least one GPU id is returned, and fetch that GPU's /telemetry and assert 200 with rows — proving CSV→streamer→MQ→collector→Postgres→gateway flowed. Use require assertions. Do NOT add kill/restart scenarios (deferred to Phase 6 per CONTEXT deferred).
    DOCKER_HOST and TESTCONTAINERS_RYUK_DISABLED are supplied by the make test-harness target (05-01) — document that at the top of the file; do not hardcode the socket path in code.
  </action>
  <verify>
    <automated>go vet -tags=e2e ./test/harness/... && go build -tags=e2e ./test/harness/... && DOCKER_HOST=unix://$HOME/.rd/docker.sock TESTCONTAINERS_RYUK_DISABLED=true go test -tags=e2e -count=1 -timeout 180s ./test/harness/...</automated>
  </verify>
  <acceptance_criteria>
    - go.mod records github.com/testcontainers/testcontainers-go/modules/compose v0.43.0 (matching testcontainers-go core v0.43.0)
    - test/harness/harness_test.go first line is `//go:build e2e` and package is harness_test
    - `go build ./...` and `go test ./...` (no tags) do NOT compile the harness (tag isolation) — running `go test ./...` shows no test/harness package
    - `make coverage` still reports total coverage >= 90% and its output does NOT list a test/harness package
    - `go build -tags=e2e ./test/harness/...` compiles; the harness calls compose.NewDockerComposeWith with ../../docker-compose.full.yml, a KEEP=1-aware t.Cleanup calling stack.Down, and asserts GET /api/v1/gpus returns 200 + non-empty array then a telemetry fetch returns 200
    - `make test` passes across all internal/ and pkg/ packages (QA-01)
  </acceptance_criteria>
  <done>The e2e harness compiles and (with Docker) runs green against the live compose stack; make test and make coverage remain green with the harness excluded (QA-01, QA-04 closed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go test process → Docker daemon | harness drives docker-compose lifecycle via the Rancher socket |
| go get → Go module proxy | testcontainers-go/modules/compose fetched from the module ecosystem |
| harness HTTP client → gateway container | assertions issued over host-mapped port 8090 |

## STRIDE Threat Register (ASVS L1, blocking threshold: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Tampering | testcontainers Ryuk reaper | low | accept | Ryuk disabled per project convention (Rancher Desktop); harness t.Cleanup performs explicit stack.Down teardown even on failure (D-18) — no orphaned containers rely on Ryuk |
| T-05-07 | Denial of Service | orphaned compose stack | low | mitigate | Hermetic up→test→down; KEEP=1 is an explicit opt-in debug escape hatch; RemoveOrphans + RemoveVolumes on teardown |
| T-05-SC | Tampering | go get testcontainers-go/modules/compose@v0.43.0 | medium | mitigate | RESEARCH Package Legitimacy Audit verdict OK — official sub-module of the already-pinned testcontainers-go v0.43.0; version pinned; go.sum digest-locked. Not ASSUMED/SUS/SLOP → no blocking checkpoint required |
</threat_model>

<verification>
- `docker compose -f docker-compose.full.yml config` valid
- `go build -tags=e2e ./test/harness/...` compiles
- `make test-harness` (Docker up) runs the harness green; teardown leaves no stack unless KEEP=1
- `make test` green; `make coverage` >= 90% with test/harness excluded
</verification>

<success_criteria>
`make test-harness` owns a full five-image + Postgres compose stack, proves end-to-end pipeline correctness against live containers, and tears down hermetically; the new e2e package does not dilute the ≥90% coverage gate; QA-01 and QA-04 hold.
</success_criteria>

<output>
Create `.planning/phases/05-devops-quality-gates/05-03-SUMMARY.md` when done
</output>