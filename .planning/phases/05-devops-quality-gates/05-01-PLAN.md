---
phase: 05-devops-quality-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build/mq.Dockerfile
  - build/streamer.Dockerfile
  - build/collector.Dockerfile
  - build/gateway.Dockerfile
  - build/migrate.Dockerfile
  - Makefile
autonomous: true
requirements: [OPS-01, OPS-05]
must_haves:
  truths:
    - "make docker builds five images tagged vantage/<svc>:dev (mq, streamer, collector, gateway, migrate) with no build error"
    - "Every final image is distroless (gcr.io/distroless/static-debian12) with no shell and a single static CGO_ENABLED=0 binary"
    - "make -n deploy, make -n soak, make -n test-harness, make -n kind-load, make -n dependency-update all resolve without a Makefile parse error"
    - "Makefile exports PATH (prepending ~/go/bin), DOCKER_HOST (Rancher socket), and TESTCONTAINERS_RYUK_DISABLED for every target"
  artifacts:
    - build/mq.Dockerfile
    - build/streamer.Dockerfile
    - build/collector.Dockerfile
    - build/gateway.Dockerfile
    - build/migrate.Dockerfile
    - Makefile
  key_links:
    - "Makefile docker-% pattern rule resolves build/%.Dockerfile → vantage/%:dev for all five images"
    - "DOCKER_IMAGES := $(SERVICES) migrate makes kind-load and deploy cover the 5th (migrate) image without adding it to SERVICES (keeps make build unchanged)"
---

<objective>
Produce the five multi-stage service/migrate Docker images and extend the Makefile with the full image-build, kind-load, deploy, soak, and test-harness plumbing. This is the container + orchestration foundation for the rest of Phase 5 (Helm deploy in 05-02, harness in 05-03, smoke/soak in 05-04 all consume these images and targets).

Purpose: OPS-01 (multi-stage Dockerfile per service) and OPS-05 (Makefile targets), plus the Makefile plumbing every later plan references so no later plan needs to edit the Makefile.
Output: build/*.Dockerfile (×5), extended Makefile.
</objective>

## Phase Goal

**As a** platform operator, **I want to** build, deploy, and validate the entire vantage pipeline on a local Kubernetes cluster with single make commands, **so that** I can run and prove the end-to-end system reproducibly.

This plan delivers the first operator-usable capability: `make docker` produces all five runnable images and every downstream `make` target parses.

## Artifacts this phase produces

New symbols/targets introduced by THIS plan (exclude from drift/source-grounding checks):
- Files: `build/mq.Dockerfile`, `build/streamer.Dockerfile`, `build/collector.Dockerfile`, `build/gateway.Dockerfile`, `build/migrate.Dockerfile`
- Makefile variables: `DOCKER_IMAGES`
- Makefile targets: `kind-load`, `deploy`, `dependency-update`, `soak`, `test-harness`
- Makefile exports: `PATH` (prepend `$(HOME)/go/bin`), `DOCKER_HOST`, `TESTCONTAINERS_RYUK_DISABLED`

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-devops-quality-gates/05-CONTEXT.md
@.planning/phases/05-devops-quality-gates/05-RESEARCH.md
@.planning/phases/05-devops-quality-gates/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Five multi-stage Dockerfiles (mq, streamer, collector, gateway, migrate)</name>
  <files>build/mq.Dockerfile, build/streamer.Dockerfile, build/collector.Dockerfile, build/gateway.Dockerfile, build/migrate.Dockerfile</files>
  <read_first>
    - .planning/phases/05-devops-quality-gates/05-RESEARCH.md (Pattern 1: Multi-Stage Dockerfile; per-service ports table)
    - .planning/phases/05-devops-quality-gates/05-PATTERNS.md (build/*.Dockerfile section — per-service substitution table)
    - Makefile (docker-% pattern rule at lines 107-110 — filenames must match build/<svc>.Dockerfile exactly)
    - cmd/gateway/main.go (confirm pkg/docs is imported as a Go package `_ "github.com/ajitg/vantage/pkg/docs"`, NOT go:embed — no extra COPY needed)
  </read_first>
  <action>
    Create five Dockerfiles under build/, one per binary, all following the shared two-stage pattern from RESEARCH Pattern 1. Stage 1 builder: FROM golang:1.26-alpine AS builder, WORKDIR /src, COPY go.mod go.sum first then RUN go mod download (layer caching), COPY . ., then RUN CGO_ENABLED=0 GOOS=linux go build -o /out/<svc> ./cmd/<svc>. Stage 2 final: FROM gcr.io/distroless/static-debian12 AS final, COPY --from=builder /out/<svc> /<svc>, ENTRYPOINT ["/<svc>"].
    Per-service specifics (from the PATTERNS substitution table): mq builds ./cmd/mq and EXPOSE 50051 8080; streamer builds ./cmd/streamer with NO EXPOSE (client-only); collector builds ./cmd/collector with NO EXPOSE (client-only); gateway builds ./cmd/gateway and EXPOSE 8080; migrate builds ./cmd/migrate with NO EXPOSE (one-shot job).
    gateway needs no extra COPY of pkg/docs — it is compiled into the binary via the Go import (verify in read_first). migrate wraps the existing cmd/migrate binary (do not re-implement migration logic — it reads VANTAGE_DB_DSN and calls pkg/db.Migrate). Filenames MUST be exactly build/mq.Dockerfile etc. so the existing Makefile docker-% rule (docker build -f build/$*.Dockerfile -t vantage/$*:dev .) resolves.
  </action>
  <verify>
    <automated>DOCKER_HOST=unix://$HOME/.rd/docker.sock docker build -f build/migrate.Dockerfile -t vantage/migrate:dev . && docker image inspect vantage/migrate:dev >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Five files exist: build/{mq,streamer,collector,gateway,migrate}.Dockerfile
    - Each file has exactly two FROM stages: `golang:1.26-alpine AS builder` and `gcr.io/distroless/static-debian12 AS final`
    - Each build line contains `CGO_ENABLED=0 GOOS=linux go build -o /out/<svc> ./cmd/<svc>` matching the file's service
    - mq.Dockerfile contains `EXPOSE 50051 8080`; gateway.Dockerfile contains `EXPOSE 8080`; streamer/collector/migrate contain no EXPOSE line
    - `docker build -f build/migrate.Dockerfile -t vantage/migrate:dev .` exits 0 and `docker image inspect vantage/migrate:dev` succeeds
  </acceptance_criteria>
  <done>All five Dockerfiles build their target binary in a distroless final stage; migrate image builds and is inspectable.</done>
</task>

<task type="auto">
  <name>Task 2: Makefile — exports, DOCKER_IMAGES, and kind-load/deploy/dependency-update/soak/test-harness targets</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (current — SERVICES at line 16, .PHONY at lines 23-25, docker-% rule 107-110, kind-up/helm-install/kind-down stubs 112-119)
    - .planning/phases/05-devops-quality-gates/05-PATTERNS.md (Makefile section — exact exports, new variable, new targets, kind-up/helm-install edits)
    - .planning/phases/05-devops-quality-gates/05-RESEARCH.md (Pattern 5 kind PATH/DOCKER_HOST; Pattern 6 kind-load; Makefile Additions Summary)
  </read_first>
  <action>
    Extend the existing Makefile (do NOT rewrite it) with the additions from the PATTERNS Makefile section. Near the top after the PROTO/PB vars and before .DEFAULT_GOAL, add three exports: `export PATH := $(HOME)/go/bin:$(PATH)`, `export DOCKER_HOST := unix:///Users/ajitg/.rd/docker.sock`, `export TESTCONTAINERS_RYUK_DISABLED := true`. Keep `SERVICES := mq streamer collector gateway` unchanged; add a new line `DOCKER_IMAGES := $(SERVICES) migrate` (migrate is the 5th image but stays out of SERVICES so `make build` still only builds the four service binaries).
    Add new targets each with a `## ` help comment: `dependency-update` runs `helm dependency update deployments/`; `kind-load` loops over DOCKER_IMAGES running `kind load docker-image vantage/$$img:dev --name vantage`; `deploy: docker kind-load helm-install` (composite per D-07); `soak` runs `bash scripts/soak.sh` (script lands in 05-04); `test-harness` runs `DOCKER_HOST=unix://$$HOME/.rd/docker.sock TESTCONTAINERS_RYUK_DISABLED=true go test -race -tags=e2e -count=1 -v -timeout 120s ./test/harness/...` (harness lands in 05-03).
    Modify `helm-install` to add `dependency-update` as a prerequisite (helm-install: dependency-update) so the Bitnami OCI chart is pulled before install. Leave `kind-up` body as `kind create cluster --name vantage` (the top-of-file exports make it find kind and the Rancher socket). Extend the existing `.PHONY` list with: kind-load deploy dependency-update soak test-harness.
    Do NOT hardcode replicas or any chart values here — this task is Makefile-only. The soak/test-harness/helm-install targets reference files created in later plans; that is expected — they parse now and run once those files land.
  </action>
  <verify>
    <automated>make -n deploy && make -n soak && make -n test-harness && make -n kind-load && make -n dependency-update && grep -q 'DOCKER_IMAGES := $(SERVICES) migrate' Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `make -n deploy` prints the docker → kind-load → helm-install chain without a Makefile parse error
    - `make -n soak`, `make -n test-harness`, `make -n kind-load`, `make -n dependency-update` each resolve without error
    - Makefile contains the three exports: `export PATH := $(HOME)/go/bin:$(PATH)`, `export DOCKER_HOST := unix:///Users/ajitg/.rd/docker.sock`, `export TESTCONTAINERS_RYUK_DISABLED := true`
    - Makefile contains `DOCKER_IMAGES := $(SERVICES) migrate` and `SERVICES := mq streamer collector gateway` is unchanged
    - `helm-install` target line lists `dependency-update` as a prerequisite
    - `.PHONY` includes kind-load deploy dependency-update soak test-harness
    - `make build` still only builds the four service binaries (migrate not built as a binary by `make build`)
  </acceptance_criteria>
  <done>All new targets parse via make -n; DOCKER_IMAGES includes migrate; helm-install depends on dependency-update; exports present; make build unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build host → OCI base images | golang:1.26-alpine and gcr.io/distroless base images pulled from public registries |
| Makefile targets → local Docker/Rancher socket | make invokes docker/kind against unix:///Users/ajitg/.rd/docker.sock |

## STRIDE Threat Register (ASVS L1, blocking threshold: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | final Docker images | medium | mitigate | distroless/static-debian12 final stage — no shell, no package manager, single static CGO_ENABLED=0 binary; minimal CVE + attack surface (D-08) |
| T-05-02 | Information Disclosure | build layers | low | accept | No secrets baked into images; DSN/credentials are injected at runtime via env (VANTAGE_DB_DSN), never at build time |
| T-05-SC | Tampering | base image pulls (golang, distroless) | medium | mitigate | Pinned tags (golang:1.26-alpine, distroless/static-debian12); no third-party runtime packages added; no npm/pip/cargo installs in this plan |
</threat_model>

<verification>
- `make docker` (with DOCKER_HOST set) builds all five vantage/*:dev images
- `docker images vantage/*:dev` lists mq, streamer, collector, gateway, migrate
- `make -n deploy soak test-harness kind-load dependency-update` all parse
- `make build` unchanged (four service binaries only)
</verification>

<success_criteria>
Five distroless images build from build/*.Dockerfile via the existing docker-% rule; Makefile exposes all Phase-5 targets and they parse; no later plan needs to edit the Makefile.
</success_criteria>

<output>
Create `.planning/phases/05-devops-quality-gates/05-01-SUMMARY.md` when done
</output>
