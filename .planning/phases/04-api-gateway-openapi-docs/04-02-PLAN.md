---
phase: 04-api-gateway-openapi-docs
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - internal/gateway/handler.go
  - internal/gateway/server.go
  - internal/gateway/handler_test.go
  - internal/gateway/integration_test.go
  - pkg/db/read.go
  - pkg/db/read_test.go
autonomous: true
requirements: [API-02, API-03]
must_haves:
  truths:
    - "GET /api/v1/gpus/{id}/telemetry returns that GPU's rows ordered by timestamp DESC (API-02)"
    - "The ?start_time=&end_time= window filters rows and its query plan uses idx_gpu_metrics_gpu_id_ts (API-03)"
    - "OQ-2 (resolved): unknown {id} -> 404; known id with zero rows in window -> 200 []"
    - "OQ-3 (resolved): partial time bounds allowed (omitted bound = unbounded that direction)"
    - "OQ-4 (resolved): malformed RFC3339 time param -> 400 {\"error\":\"...\"}"
    - "OQ-1 (resolved): result set capped at VANTAGE_GATEWAY_MAX_ROWS (default 1000)"
  artifacts:
    - pkg/db/read.go
    - internal/gateway/handler.go
    - internal/gateway/integration_test.go
  key_links:
    - "handler.GetTelemetry -> db.GPUExists (404 gate) -> db.Telemetry (windowed query, LIMIT maxRows)"
    - "db.Telemetry predicate WHERE gpu_id=$1 [AND timestamp range] ORDER BY timestamp DESC -> idx_gpu_metrics_gpu_id_ts"
---

<objective>
Ship the telemetry vertical slice: `GET /api/v1/gpus/{id}/telemetry` returns a GPU's
time-series rows ordered newest-first (API-02), and the optional `?start_time=&end_time=`
window filters by RFC3339 bounds using the composite index `idx_gpu_metrics_gpu_id_ts`
(API-03). Resolves the open questions: 404 for unknown GPU (OQ-2), partial bounds (OQ-3),
400 on malformed time (OQ-4), and a configurable result cap (OQ-1).

Purpose: This is the primary read capability of the gateway — turning stored telemetry into
a queryable HTTP resource and proving the Phase-2 composite index is actually exercised.
Output: A complete telemetry endpoint plus the windowed read-query layer in `pkg/db`, with
an integration test that asserts the EXPLAIN plan is an Index Scan on the composite index.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-api-gateway-openapi-docs/04-RESEARCH.md
@.planning/phases/04-api-gateway-openapi-docs/04-01-SUMMARY.md

# Source patterns to mirror
@pkg/models/telemetry.go
@pkg/db/migrations/000001_init_schema.up.sql
</context>

## Phase Goal

**As an** operator, **I want to** retrieve a GPU's telemetry over a time window via the REST API,
**so that** I can inspect its readings without writing SQL and trust the query uses the composite index.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing tests for the telemetry endpoint + window + index proof</name>
  <files>internal/gateway/integration_test.go, internal/gateway/handler_test.go, pkg/db/read_test.go</files>
  <behavior>
    - TestGetTelemetry_NoFilter: seeded GPU returns its rows as JSON, ordered timestamp DESC (API-02).
    - TestGetTelemetry_TimeWindow: start_time/end_time bounds return only in-range rows (API-03).
    - TestGetTelemetry_PartialBounds: only start_time (unbounded upper) and only end_time (unbounded lower) each filter correctly (OQ-3).
    - TestGetTelemetry_UnknownGPU: an id absent from DISTINCT gpu_id returns 404 + ErrorResponse (OQ-2).
    - TestGetTelemetry_KnownGPUEmptyWindow: known id with no rows in the window returns 200 + [] (OQ-2).
    - TestGetTelemetry_BadTime: malformed start_time returns 400 + ErrorResponse (OQ-4).
    - TestGetTelemetry_ResultCap: with VANTAGE_GATEWAY_MAX_ROWS small, response row count == cap (OQ-1).
    - TestTelemetry_UsesCompositeIndex: run EXPLAIN on the windowed query and assert the plan text contains idx_gpu_metrics_gpu_id_ts and an Index Scan (API-03 / DB-02).
  </behavior>
  <action>
    Extend internal/gateway/integration_test.go (package gateway_test, `//go:build integration`) with the tests above, all routed through gateway.NewRouter via httptest so chi.URLParam resolves {id} (RESEARCH Pitfall 2 — never call the handler directly). Seed rows with distinct timestamps for ordering and window assertions, reusing the TestMain pool + Snapshot/Restore from Plan 01. Add TestTelemetry_UsesCompositeIndex to pkg/db/read_test.go: execute `EXPLAIN (ANALYZE, FORMAT TEXT) <the windowed telemetry SQL>` against the seeded pool and assert the returned plan lines contain "idx_gpu_metrics_gpu_id_ts". Add handler_test.go cases for the 400 malformed-time path (routed). These reference db.Telemetry, db.GPUExists, and the cap config that do not yet exist — RED.
  </action>
  <verify>
    <automated>cd /Users/ajitg/workspace/vantage && go vet ./internal/gateway/... ./pkg/db/... 2>&1 | grep -q . && echo "RED" || echo "RED-CHECK"</automated>
  </verify>
  <done>New telemetry tests exist and reference db.Telemetry / db.GPUExists; packages do not yet compile (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the telemetry read layer + handler to GREEN</name>
  <files>pkg/db/read.go, internal/gateway/handler.go, internal/gateway/server.go</files>
  <behavior>
    - db.Telemetry returns []models.GpuMetric ordered timestamp DESC, capped at limit, with optional inclusive bounds.
    - db.GPUExists reports whether an id appears in gpu_metrics.
    - GetTelemetry maps to []GpuMetricResponse; 404 unknown id; 400 bad time; 200 [] empty window.
  </behavior>
  <action>
    Extend pkg/db/read.go with `GPUExists(ctx, pool, id string) (bool, error)` (SELECT EXISTS(SELECT 1 FROM gpu_metrics WHERE gpu_id=$1)) and `Telemetry(ctx, pool, id string, start, end *time.Time, limit int) ([]models.GpuMetric, error)`. Use the two-query approach (RESEARCH Pattern 5 / A1): when both bounds are nil, `WHERE gpu_id=$1 ORDER BY timestamp DESC LIMIT $2`; otherwise apply the nullable-bound predicate `AND ($2::timestamptz IS NULL OR timestamp >= $2) AND ($3::timestamptz IS NULL OR timestamp <= $3)` so partial bounds work (OQ-3), keeping `ORDER BY timestamp DESC LIMIT` so the planner uses idx_gpu_metrics_gpu_id_ts (API-03). Scan into models.GpuMetric (no JSON tags); return a non-nil empty slice when no rows. Never format the DSN into errors.
    Extend internal/gateway/handler.go: implement `GetTelemetry(pool *pgxpool.Pool, maxRows int) http.HandlerFunc` that reads chi.URLParam(r,"id"); parses start_time/end_time with time.Parse(time.RFC3339, v) when present, returning 400 + ErrorResponse on parse failure (OQ-4); calls db.GPUExists and returns 404 + ErrorResponse when the id is unknown (OQ-2); calls db.Telemetry with limit=maxRows (OQ-1) and json-encodes the mapped []GpuMetricResponse; 200 + [] when the window is empty for a known id. Add the full swag annotation block above GetTelemetry (path/query @Param for id/start_time/end_time, @Success 200 {array} GpuMetricResponse, @Failure 400/404/500 {object} ErrorResponse, @Router /gpus/{id}/telemetry [get]) per OQ-5.
    Extend internal/gateway/server.go NewRouter to register `r.Get("/{id}/telemetry", GetTelemetry(pool, cfg.MaxRows))` inside the /api/v1/gpus route group.
    Run unit + integration tests to GREEN.
  </action>
  <verify>
    <automated>cd /Users/ajitg/workspace/vantage && go build ./internal/gateway/... && go test -race ./internal/gateway/... ./pkg/db/... -count=1 && DOCKER_HOST=unix://$HOME/.rd/docker.sock TESTCONTAINERS_RYUK_DISABLED=true go test -race -tags=integration -run 'TestGetTelemetry|TestTelemetry_UsesCompositeIndex' ./internal/gateway/... ./pkg/db/... -count=1</automated>
  </verify>
  <done>All telemetry tests pass: ordered rows (API-02), windowed filter (API-03) with EXPLAIN proving Index Scan on idx_gpu_metrics_gpu_id_ts, 404 unknown id (OQ-2), 200 [] empty window (OQ-2), 400 malformed time (OQ-4), result capped at VANTAGE_GATEWAY_MAX_ROWS (OQ-1).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → gateway | Untrusted {id} path var + start_time/end_time query params cross here |
| gateway → PostgreSQL | Parameterized windowed read crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | GetTelemetry {id} path param | high | mitigate | id is bound as $1 in pgxpool.Query — never string-concatenated into SQL; injection impossible (ASVS V5) |
| T-04-04 | Tampering | start_time/end_time query params | high | mitigate | Values parsed by time.Parse(RFC3339); only the resulting time.Time is bound as $2/$3; raw string never reaches SQL (ASVS V5) |
| T-04-05 | Denial of Service | unbounded telemetry result | high | mitigate | LIMIT maxRows (VANTAGE_GATEWAY_MAX_ROWS default 1000) caps every response (OQ-1) |
| T-04-06 | Information Disclosure | db.Telemetry error path | medium | mitigate | Errors wrap context only; DSN never formatted into error strings (ASVS V8) |
</threat_model>

<verification>
- `go test -race ./internal/gateway/... ./pkg/db/... -count=1` passes (unit incl. 400 path).
- Integration suite passes, including the EXPLAIN index-usage assertion (API-03 / DB-02).
- `make test` passes across the module (wave-merge gate).
</verification>

<success_criteria>
The telemetry endpoint returns ordered rows (API-02), filters by an RFC3339 window backed by the
composite index (API-03, EXPLAIN-proven), and honors the resolved open questions: 404 unknown id,
200 [] empty window, 400 malformed time, and a configurable result cap.
</success_criteria>

<output>
Create `.planning/phases/04-api-gateway-openapi-docs/04-02-SUMMARY.md` when done.
</output>
