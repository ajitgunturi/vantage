---
phase: 04-api-gateway-openapi-docs
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - cmd/gateway/main.go
  - pkg/docs/docs.go
  - pkg/docs/swagger.json
  - pkg/docs/swagger.yaml
  - internal/gateway/swagger_test.go
  - Makefile
  - scripts/smoke/phase04-gateway.sh
  - README.md
autonomous: true
requirements: [API-04]
user_setup:
  - service: swag-cli
    why: "swag init generates pkg/docs from gateway annotations (build-time tool, not a go.mod dep)"
    dashboard_config:
      - task: "Install pinned swag CLI: go install github.com/swaggo/swag/cmd/swag@v1.16.6"
        location: "local shell (executor runs this in Task 1)"
must_haves:
  truths:
    - "make swagger regenerates pkg/docs entirely from code annotations — no hand-written spec (API-04)"
    - "Swagger UI serves at /swagger/ and /swagger/doc.json is valid JSON with >=3 documented paths"
    - "cmd/gateway/main.go boots the gateway (db.Migrate -> db.New -> NewRouter) under an errgroup"
    - "make coverage excludes pkg/docs (generated) and the module stays >=90% line coverage"
    - "OQ-5 (resolved): file-level @title/@version/@BasePath in main.go; handler @Summary/@Router in internal/gateway"
  artifacts:
    - cmd/gateway/main.go
    - pkg/docs/swagger.json
    - scripts/smoke/phase04-gateway.sh
    - README.md
  key_links:
    - "cmd/gateway/main.go side-effect import _ pkg/docs -> registers spec -> httpSwagger serves /swagger/doc.json"
    - "Makefile coverage grep -v '/pkg/pb\\|/pkg/docs' -> generated code excluded from the gate"
---

<objective>
Complete the gateway as a runnable, documented service: build the composition root
`cmd/gateway/main.go`, auto-generate the OpenAPI spec into `pkg/docs` from the swag
annotations written in Plans 01–02 (API-04), serve Swagger UI, extend the coverage gate to
exclude the generated `pkg/docs`, and add the Phase-4 smoke script + README section.

Purpose: Turn the two endpoint slices into a process a user can actually start, hit, and
read live API docs for — and make `make build test coverage lint` and `make swagger` all
green so the phase ships behind its quality gates.
Output: A booting gateway binary, a generated-and-served OpenAPI spec, a fixed coverage gate,
a curl smoke check, and a README quickstart for the gateway.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-api-gateway-openapi-docs/04-RESEARCH.md
@.planning/phases/04-api-gateway-openapi-docs/04-01-SUMMARY.md
@.planning/phases/04-api-gateway-openapi-docs/04-02-SUMMARY.md

# Source patterns to mirror
@cmd/mq/main.go
@Makefile
@scripts/smoke/phase03-pipeline.sh
</context>

## Phase Goal

**As an** operator, **I want to** start the gateway and read its auto-generated OpenAPI docs in a browser,
**so that** I can run the API and discover its endpoints without reading the source.

<tasks>

<task type="auto">
  <name>Task 1: Composition root + generate the OpenAPI spec (API-04)</name>
  <files>cmd/gateway/main.go, pkg/docs/docs.go, pkg/docs/swagger.json, pkg/docs/swagger.yaml</files>
  <action>
    Install the pinned tool: `go install github.com/swaggo/swag/cmd/swag@v1.16.6` (CLAUDE.md pins v1.16.6, not the `make tools` @latest — RESEARCH Environment Availability).
    Create cmd/gateway/main.go (package main) mirroring cmd/mq/main.go: the file-level swag header annotations (@title "Vantage GPU Telemetry API", @version 1.0, @description, @BasePath /api/v1, @host localhost:8080, @schemes http) immediately above `package main` (OQ-5); a main() that signal.NotifyContext(SIGTERM/SIGINT), db.FromEnv -> db.Migrate -> db.New (defer pool.Close), gateway.FromEnv, gateway.NewRouter(pool, cfg), wraps an http.Server (ReadTimeout 5s / WriteTimeout 10s) and runs ListenAndServe + Shutdown under errgroup.WithContext (RESEARCH Pattern 8). Never log the DB config/DSN (ASVS V8).
    Generation order (RESEARCH Pitfall 4): FIRST write main.go WITHOUT the `_ "github.com/ajitg/vantage/pkg/docs"` import, run `make swagger` to generate pkg/docs, THEN add the side-effect blank import and rebuild — this avoids the import-before-generate compile failure.
    Commit the generated pkg/docs files (they are committed, not gitignored — RESEARCH Recommended Project Structure).
  </action>
  <verify>
    <automated>cd /Users/ajitg/workspace/vantage && make swagger && test -f pkg/docs/swagger.json && go build ./cmd/gateway/... && python3 -c "import json,sys; d=json.load(open('pkg/docs/swagger.json')); n=len(d.get('paths',{})); print('paths',n); sys.exit(0 if n>=2 else 1)"</automated>
  </verify>
  <done>`make swagger` regenerates pkg/docs from annotations (no hand-written spec); cmd/gateway builds with the docs side-effect import; swagger.json documents the gpus + telemetry paths (API-04).</done>
</task>

<task type="auto">
  <name>Task 2: Fix coverage gate + serve-spec test</name>
  <files>Makefile, internal/gateway/swagger_test.go</files>
  <action>
    Update the Makefile coverage target: change the package filter from `grep -v '/pkg/pb'` to `grep -v '/pkg/pb\|/pkg/docs'` so the generated pkg/docs (0%-coverage generated code, like pkg/pb) is excluded from the >=90% gate (RESEARCH Makefile Coverage Gate fix / Pitfall 5). Change only that line.
    Create internal/gateway/swagger_test.go (package gateway_test, no build tag) with TestSwaggerUI: it must import the generated spec as a side effect (`_ "github.com/ajitg/vantage/pkg/docs"`), serve gateway.NewRouter via httptest, GET /swagger/doc.json, assert 200 + a valid-JSON body whose `paths` object has >=2 entries, and GET /swagger/index.html returns 200. This proves the spec is registered and served (API-04 runtime path; RESEARCH Pitfall 1).
    Run the coverage gate.
  </action>
  <verify>
    <automated>cd /Users/ajitg/workspace/vantage && go test -race -run TestSwaggerUI ./internal/gateway/... -count=1 && DOCKER_HOST=unix://$HOME/.rd/docker.sock TESTCONTAINERS_RYUK_DISABLED=true make coverage</automated>
  </verify>
  <done>TestSwaggerUI passes (spec served at /swagger/doc.json with >=2 paths); `make coverage` passes at >=90% with pkg/docs excluded.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-4 smoke script + README quickstart</name>
  <files>scripts/smoke/phase04-gateway.sh, README.md</files>
  <action>
    Create scripts/smoke/phase04-gateway.sh (executable, set -euo pipefail) modeled on scripts/smoke/phase03-pipeline.sh: against the `make dev-up` Postgres, build and start the gateway binary in the background (export VANTAGE_DB_DSN to the compose DSN and GATEWAY_ADDR), wait for readiness, then curl-assert: (1) GET /api/v1/gpus returns HTTP 200 + a JSON array; (2) GET /api/v1/gpus/<first-id>/telemetry returns 200 + array; (3) GET /api/v1/gpus/GPU-does-not-exist/telemetry returns 404; (4) GET /swagger/doc.json returns 200 + valid JSON. Use the pg_exec docker-compose-exec helper pattern (no host psql) to discover a seeded gpu_id, and kill only the gateway process on exit. Make it idempotent and wire-compatible with `make smoke-04`.
    Extend README.md with an "API Gateway" quickstart section (grown incrementally per DOC-01): how to run `make dev-up` + start the gateway, the three endpoints with example curl, the time-window query params, and the Swagger UI URL (http://localhost:8080/swagger/). Reference `make smoke-04`.
  </action>
  <verify>
    <automated>cd /Users/ajitg/workspace/vantage && bash -n scripts/smoke/phase04-gateway.sh && test -x scripts/smoke/phase04-gateway.sh && grep -q 'swagger' README.md && grep -q '/api/v1/gpus' README.md</automated>
  </verify>
  <done>`scripts/smoke/phase04-gateway.sh` is an executable, syntactically valid smoke check runnable via `make smoke-04`; README documents the gateway endpoints + Swagger UI (DOC-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → /swagger/* | Untrusted requests hit the embedded Swagger UI + spec |
| build-time → pkg/docs | Generated spec is committed and imported by the binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Information Disclosure | cmd/gateway/main.go startup logs | medium | mitigate | main() logs only the listen address; db.Config/DSN is never logged or formatted into errors (ASVS V8) |
| T-04-08 | Tampering | pkg/docs generated spec | low | accept | Spec is build-time generated from annotations and committed; no runtime mutation; regenerated deterministically by make swagger |
| T-04-09 | Denial of Service | /swagger/* static assets | low | accept | http-swagger serves embedded static UI (go:embed); no DB or unbounded work on this path |
</threat_model>

<verification>
- `make swagger` regenerates pkg/docs cleanly from annotations (API-04).
- `make build && make test && make coverage && make lint` all pass (coverage excludes pkg/docs).
- `bash -n scripts/smoke/phase04-gateway.sh` valid; `make smoke-04` runs the curl checks against the dev stack.
- README documents the gateway endpoints + Swagger UI (DOC-01).
</verification>

<success_criteria>
The gateway binary boots and serves an auto-generated OpenAPI spec (API-04) with live Swagger UI;
the coverage gate excludes generated pkg/docs and the module holds >=90%; the Phase-4 smoke script
and README quickstart are in place. All four make gates are green.
</success_criteria>

<output>
Create `.planning/phases/04-api-gateway-openapi-docs/04-03-SUMMARY.md` when done.
</output>
