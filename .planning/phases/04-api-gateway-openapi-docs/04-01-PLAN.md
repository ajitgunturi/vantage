---
phase: 04-api-gateway-openapi-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - internal/gateway/config.go
  - internal/gateway/handler.go
  - internal/gateway/server.go
  - internal/gateway/handler_test.go
  - internal/gateway/integration_test.go
  - pkg/db/read.go
  - pkg/db/read_test.go
autonomous: true
requirements: [API-01]
must_haves:
  truths:
    - "GET /api/v1/gpus returns a JSON array of distinct gpu_id UUID strings from PostgreSQL (API-01)"
    - "An empty gpu_metrics table yields HTTP 200 with [] (not 404, not null)"
    - "The gateway reuses pkg/db pgxpool — it never opens its own pool"
    - "OQ-1 (resolved): config exposes VANTAGE_GATEWAY_MAX_ROWS (default 1000) for review"
  artifacts:
    - internal/gateway/config.go
    - internal/gateway/handler.go
    - internal/gateway/server.go
    - pkg/db/read.go
    - internal/gateway/integration_test.go
  key_links:
    - "handler.ListGPUs -> db.DistinctGPUIDs -> pgxpool.Query (SELECT DISTINCT gpu_id ... ORDER BY gpu_id)"
    - "NewRouter mounts GET /api/v1/gpus and a /swagger/* handler stub"
---

<objective>
Stand up the API Gateway service skeleton and ship its first vertical slice: a working
`GET /api/v1/gpus` endpoint that returns the distinct list of GPU UUIDs from PostgreSQL,
served by a chi router over the shared `pkg/db` pgxpool (API-01).

Purpose: Deliver an end-to-end, user-observable capability (list the GPUs that have
reported telemetry) on top of which the telemetry and OpenAPI slices build. This slice
also lands the gateway-local config, the response/error DTOs, and the reusable read-query
layer in `pkg/db`.
Output: A booting chi router with one live data endpoint, a tested `pkg/db` read function,
and the dependency additions (chi/v5, http-swagger/v2) the later slices need.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-api-gateway-openapi-docs/04-RESEARCH.md

# Source patterns to mirror
@pkg/db/db.go
@pkg/db/config.go
@pkg/models/telemetry.go
@internal/http/inspect.go
@internal/collector/run_test.go
</context>

## Phase Goal

**As an** operator, **I want to** query the list of GPUs that have reported telemetry over a
documented REST API, **so that** I can discover available GPU UUIDs without querying PostgreSQL directly.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing end-to-end test for GET /api/v1/gpus</name>
  <files>internal/gateway/integration_test.go, internal/gateway/handler_test.go, pkg/db/read_test.go</files>
  <behavior>
    - integration: testcontainers postgres:17-alpine + db.Migrate, seed three rows across two distinct gpu_id UUIDs; GET /api/v1/gpus via chi router returns 200 and a JSON array of exactly those two UUIDs, sorted ascending.
    - integration: empty table returns 200 with body `[]` (a literal empty JSON array), Content-Type application/json.
    - unit: a request routed through NewRouter to /api/v1/gpus sets Content-Type application/json.
    - pkg/db unit/integration: DistinctGPUIDs against a seeded pool returns the de-duplicated, sorted UUID slice.
  </behavior>
  <action>
    Create internal/gateway/integration_test.go with `//go:build integration` and package gateway_test.
    Add a TestMain that starts postgres:17-alpine via testcontainers (postgres.Run with WithDatabase/WithUsername/WithPassword/BasicWaitStrategies/WithSQLDriver("pgx")), runs db.Migrate on the connection string, takes a Snapshot, opens a pool via db.New, and stores it in a package var — mirror internal/collector run_test.go and the RESEARCH Integration Test Pattern exactly. Add TestListGPUs_TwoGPUs and TestListGPUs_Empty that seed via direct INSERTs (using models.InsertSQL) and assert through gateway.NewRouter served by httptest. Add internal/gateway/handler_test.go (no build tag) asserting routing + Content-Type using a nil/closed pool path that still exercises NewRouter wiring (route registration only). Add pkg/db/read_test.go exercising DistinctGPUIDs behind the integration tag against the seeded pool. These MUST fail to compile/run now because the production symbols do not yet exist — that is the RED state.
    Reference: API-01. Seed UUIDs must look like real GPU UUIDs (e.g. GPU-aaaaaaaa-... and GPU-bbbbbbbb-...), never ordinals, per the pkg/models D-04 invariant.
  </action>
  <verify>
    <automated>cd /Users/ajitg/workspace/vantage && go vet ./internal/gateway/... 2>&1 | grep -q . && echo "RED: symbols undefined as expected" || echo "RED-CHECK"</automated>
  </verify>
  <done>Test files exist and reference gateway.NewRouter, db.DistinctGPUIDs, gateway.Config; the package does not yet compile because production code is absent (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bootstrap deps + implement the GPU-list slice to GREEN</name>
  <files>go.mod, go.sum, internal/gateway/config.go, internal/gateway/handler.go, internal/gateway/server.go, pkg/db/read.go</files>
  <behavior>
    - GET /api/v1/gpus returns 200 + sorted JSON array of distinct gpu_id values.
    - Empty table returns 200 + `[]`.
    - On DB error the handler returns 500 with body {"error":"..."} via ErrorResponse.
  </behavior>
  <action>
    Add dependencies: run `go get github.com/go-chi/chi/v5@v5.3.0` and `go get github.com/swaggo/http-swagger/v2@v2.0.2`, then `go mod tidy` (versions locked by CLAUDE.md / RESEARCH Standard Stack).
    Create pkg/db/read.go (package db) with `DistinctGPUIDs(ctx context.Context, pool *pgxpool.Pool) ([]string, error)` issuing `SELECT DISTINCT gpu_id FROM gpu_metrics ORDER BY gpu_id`, scanning into a []string, checking rows.Err(), and never embedding the DSN in any error (ASVS V8). Return an empty non-nil slice when there are no rows so the JSON encodes as [] not null.
    Create internal/gateway/config.go (package gateway): a Config struct with Addr string and MaxRows int, and FromEnv() reading GATEWAY_ADDR (default ":8080") and VANTAGE_GATEWAY_MAX_ROWS (default 1000) — the MaxRows field resolves OQ-1 as a configurable safety ceiling (not pagination); document that in a doc comment. Reject a non-positive or non-integer VANTAGE_GATEWAY_MAX_ROWS with an error, mirroring pkg/db.FromEnv.
    Create internal/gateway/handler.go (package gateway): define the local response DTOs `GpuMetricResponse` (JSON-tagged, per RESEARCH Pattern 6 — do NOT add JSON tags to pkg/models.GpuMetric) and `ErrorResponse{Error string `json:"error"`}`. Implement `ListGPUs(pool *pgxpool.Pool) http.HandlerFunc` that calls db.DistinctGPUIDs and json-encodes the slice; on error write 500 + ErrorResponse. Add the swag annotation block above ListGPUs (@Summary/@Tags gpus/@Produce json/@Success 200 {array} string/@Failure 500 {object} ErrorResponse/@Router /gpus [get]) — these are consumed in Plan 03 but must live with the handler now (OQ-5).
    Create internal/gateway/server.go (package gateway): `NewRouter(pool *pgxpool.Pool, cfg Config) http.Handler` building chi.NewRouter with middleware.Logger + middleware.Recoverer, `r.Route("/api/v1/gpus", ...)` registering `r.Get("/", ListGPUs(pool))` (the telemetry sub-route is added in Plan 02), and `r.Get("/swagger/*", httpSwagger.Handler(httpSwagger.URL("/swagger/doc.json")))`. Do NOT import pkg/docs here — the side-effect import lives only in cmd/gateway/main.go (Plan 03).
    Run the unit + integration tests to GREEN.
  </action>
  <verify>
    <automated>cd /Users/ajitg/workspace/vantage && go build ./internal/gateway/... && go test -race ./internal/gateway/... ./pkg/db/... -count=1 && DOCKER_HOST=unix://$HOME/.rd/docker.sock TESTCONTAINERS_RYUK_DISABLED=true go test -race -tags=integration -run 'TestListGPUs|TestDistinctGPUIDs' ./internal/gateway/... ./pkg/db/... -count=1</automated>
  </verify>
  <done>Unit + integration tests pass; GET /api/v1/gpus returns sorted distinct UUIDs (200) and [] for an empty table; chi/v5 and http-swagger/v2 are pinned in go.mod; no DSN appears in any error path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → gateway | Untrusted query string + path crosses here |
| gateway → PostgreSQL | Parameterized read queries cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | db.DistinctGPUIDs error path | medium | mitigate | Errors wrap context only; DSN is never formatted into error strings (ASVS V8); gateway never logs Config DSN |
| T-04-02 | Denial of Service | GET /api/v1/gpus | low | accept | DISTINCT result is bounded by GPU count (small cardinality); cap not required on this endpoint — documented trade-off (RESEARCH Pitfall 3) |
| T-04-SC | Tampering | go get chi/http-swagger | low | accept | Both packages CLI-locked by CLAUDE.md and approved in RESEARCH Package Legitimacy Audit (pkg.go.dev verified); Go modules, no npm/pip/cargo gate applies |
</threat_model>

<verification>
- `go build ./internal/gateway/...` succeeds (service compiles).
- `go test -race ./internal/gateway/... ./pkg/db/... -count=1` passes (unit).
- `DOCKER_HOST=... TESTCONTAINERS_RYUK_DISABLED=true go test -tags=integration ...` passes (API-01).
</verification>

<success_criteria>
GET /api/v1/gpus returns the sorted distinct list of GPU UUIDs from PostgreSQL with HTTP 200,
returns [] for an empty table, reuses the shared pgxpool, and is proven by a testcontainers
integration test. chi/v5 and http-swagger/v2 are added and pinned.
</success_criteria>

<output>
Create `.planning/phases/04-api-gateway-openapi-docs/04-01-SUMMARY.md` when done.
</output>
