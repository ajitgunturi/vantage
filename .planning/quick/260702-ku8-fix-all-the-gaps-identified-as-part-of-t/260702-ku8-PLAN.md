---
phase: quick-260702-ku8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/collector/collector.go
  - internal/streamer/streamer.go
  - pkg/db/db.go
  - test/e2e/pipeline_test.go
  - internal/server/server.go
  - cmd/mq/main.go
  - internal/gateway/handler.go
  - pkg/db/read.go
  - internal/gateway/integration_test.go
  - Makefile
  - README.md
  - docs/adr/ADR-002-natural-key-microsecond-collision.md
  - docs/AI_USAGE.md
  - .planning/REQUIREMENTS.md
  - pkg/docs
autonomous: true
requirements: [COLL-02, COLL-05, STREAM-01, MQ-09, MQ-10, API-02, API-03, QA-03, QA-04, DOC-02]

must_haves:
  truths:
    - "A batch whose DB insert fails is NOT acked; the error propagates and the broker redelivers on disconnect (no silent data loss)."
    - "A Streamer whose Produce call fails retries with exponential backoff instead of exiting; the process survives an MQ blip."
    - "A single parked consumer receives the last message of a burst even when Produce races the park (no missed wakeup)."
    - "MQ shutdown under an active Consume stream completes within a bounded time (no hang until SIGKILL)."
    - "Gateway signals truncation via X-Truncated when a telemetry result hits MaxRows."
    - "make lint fails the build when golangci-lint reports findings (no silent go vet fallback)."
    - "docs/AI_USAGE.md exists and REQUIREMENTS.md tracks DOC-02."
    - "make build && make test && make coverage && make lint all pass; coverage >= 90%."
  artifacts:
    - docs/adr/ADR-002-natural-key-microsecond-collision.md
    - docs/AI_USAGE.md
  key_links:
    - "persistBatch error path -> flush return -> Consume return -> Run reconnect loop -> broker requeue-on-disconnect"
    - "notifyChan() captured before TryDequeue -> park on captured channel"
    - "shutdownCh close -> Consume send-loop select -> stream returns -> GracefulStop unblocks"
    - "swag annotations -> make swagger -> pkg/docs regenerated"
---

<objective>
Close every CRITICAL/MAJOR/MEDIUM finding and the cheap MINOR items from the
4-agent mid-assignment review of vantage (phases 1–4). The authoritative task
spec is `260702-ku8-REVIEW-FINDINGS.md` in this directory — each task below names
the finding IDs it closes so the executor can check them off against that file.

Purpose: eliminate silent data loss (C-1), liveness bugs (M-2/M-3), and the
missing assignment deliverable (G-4 AI-usage docs), and restore honest gates.
Output: corrected pipeline + MQ + gateway code, a new ADR, AI-usage docs, an
honest lint gate, and a strengthened + wired e2e test. All work respects the
REVIEW-FINDINGS "OUT OF SCOPE" and "Constraints" sections (no schema change, no
Dockerfiles/Helm, no pagination, no /healthz — those are Phase 5/6).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260702-ku8-fix-all-the-gaps-identified-as-part-of-t/260702-ku8-REVIEW-FINDINGS.md
@CLAUDE.md
@docs/adr/ADR-001-bidi-at-least-once-delivery.md
@internal/collector/collector.go
@internal/server/server.go
@internal/streamer/streamer.go
@internal/gateway/handler.go
@pkg/db/read.go
@cmd/mq/main.go
@test/e2e/pipeline_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pipeline correctness — ack-on-persist (C-1), Streamer retry (M-6), backoff escalation (G-1), e2e strengthening (M-1), collector/streamer/db minors</name>
  <files>internal/collector/collector.go, internal/collector/collector_test.go, internal/streamer/streamer.go, internal/streamer/streamer_test.go, pkg/db/db.go, test/e2e/pipeline_test.go, Makefile, README.md</files>
  <behavior>
    - persistBatch returns a non-nil error when any br.Exec() fails OR br.Close() errors; the error carries context but never the DSN. On error NO acks are sent for that batch (whole batch is one implicit pgx transaction — it rolled back).
    - Bad-proto rows are still skipped-and-continued (never queued) and, when the rest of the batch persists cleanly, the surrounding batch is still acked — this poison-message behavior is deliberate and must be preserved with an explaining comment.
    - flush() already returns on persistBatch error; on that error Consume returns so Run's reconnect loop redelivers (do not spin re-persisting the same batch forever). The best-effort final flush on recv-done (msgCh closed) may stay best-effort.
    - Streamer Produce failure retries with exponential backoff (base 100ms, cap 5s), ctx-aware sleep, exiting only on ctx cancel — mirroring the Collector reconnect loop. An MQ that fails then "recovers" (fake client) results in eventual success, not log.Fatal.
    - Collector Run backoff resets only after a Consume attempt that survived a minimum duration (e.g. >5s) or a successful first Recv — not unconditionally at the top of every iteration.
    - e2e assertion is exact: the 200-distinct-key fixture asserts count(*) == 200 (not just > 0), genuinely proving no-loss.
  </behavior>
  <action>
Close C-1, M-6, G-1, M-1 and the listed pipeline MINORs. Read REVIEW-FINDINGS for
exact line refs before editing.

C-1 (internal/collector/collector.go persistBatch/flush): make persistBatch have a
real error return. Drain all b.Len() Exec calls (never under/over-drain — that
corrupts the pooled connection), capturing the first genuine Exec error; then check
the br.Close() error; return the first non-nil of the two, wrapped with
"collector: persist batch:" context and NO DSN. Correct the stale head-comment that
claims "the rest of the batch always lands" — pgx v5 SendBatch runs the whole batch
in one implicit transaction, so an error rolls earlier rows back too. Add a comment
at the flush ack loop stating that acking the whole batch (including skipped
bad-proto rows) is deliberate so poison messages are not redelivered forever. Verify
flush() returns on persistBatch error and that Consume returns that error (so Run
reconnects); it must not loop re-persisting the same batch.

C-1 TDD (collector_test.go): write the test FIRST. Drive persistBatch/flush into a
genuine SendBatch failure (cancelled ctx, closed pool, or a constraint-violating row
plus a real error) using a fake MQServiceClient stream that records acks; assert
zero acks were sent for the failed batch AND the error propagates out of Consume.
Keep it runnable under the existing integration tag + Rancher Docker env if it needs
a real pool.

M-6 (internal/streamer/streamer.go): wrap the Produce call (or Stream within Run) in
retry-with-backoff: exponential base 100ms cap 5s, ctx-aware sleep, exit only on ctx
cancel. One MQ blip must not kill the instance. Also make the inter-row
time.Sleep(loopDelayMS) ctx-aware (select on time.After vs ctx.Done). TDD
(streamer_test.go): fake client whose Produce fails N times then succeeds; assert the
stream retries and completes rather than returning fatal.

G-1 (internal/collector/collector.go Run): reset backoff only after a Consume attempt
that survived a minimum duration (>5s) or after a successful first Recv — track start
time around the Consume call. grpc.NewClient is lazy so dial "success" is not a real
connection; the current unconditional reset pins retries at 100ms forever.

Collector MINOR (Consume): add `ctx, cancel := context.WithCancel(ctx); defer cancel()`
at the top of Consume so the recv goroutine cannot leak on an early error return.

db MINOR (pkg/db/db.go Migrate): the postgres->pgx5 scheme rewrite misses
`postgresql://` DSNs — rewrite both `postgres://` and `postgresql://` to `pgx5://`.

M-1 (test/e2e/pipeline_test.go): the fixture inserts 200 distinct-key rows; change
Assertion 1 from count(*) > 0 to require count(*) == 200 exactly (keep the distinct
== total assertion). Add a Makefile `e2e` target that runs `./test/e2e/...` with the
integration tag and documents the Rancher Docker env, and fix the README claim
(around the exactly-once section) so it does not assert the e2e runs under a target
that did not exist. Do not fold e2e into `make coverage` if that would drag the
Docker requirement into the coverage gate — a dedicated `e2e` target is cleaner.
  </action>
  <verify>
    <automated>DOCKER_HOST=unix://$HOME/.rd/docker.sock TESTCONTAINERS_RYUK_DISABLED=true go test -race -tags=integration ./internal/collector/... ./internal/streamer/... ./pkg/db/...</automated>
  </verify>
  <done>C-1, M-6, G-1, M-1 and the collector/streamer/db MINORs are closed; new tests fail before the fix and pass after; persistBatch error path proven to send zero acks; streamer survives a simulated MQ blip; `make e2e` runs the strengthened e2e (count==200) green under the Rancher Docker env.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MQ liveness — missed-wakeup (M-2), shutdown hang (M-3), MQ minors, all under -race</name>
  <files>internal/server/server.go, internal/server/server_test.go, cmd/mq/main.go</files>
  <behavior>
    - A single consumer parked after a missed TryDequeue still receives a message produced in the race gap: notifyChan() is captured BEFORE TryDequeue and the park waits on the captured channel.
    - MQ shutdown with an active Consume stream returns promptly: the send-loop blocking selects observe shutdownCh close and return codes.Unavailable; existing defer requeues leases. main bounds GracefulStop with a timeout falling back to Stop().
    - Disconnect cleanup drains and applies pending acks FIRST, then requeues only the survivors; the dead `if _, ok := leases[id]` branch is removed.
    - A clean client CloseSend (recv sees io.EOF) makes Consume return nil, so the client sees a clean close, not codes.Unknown.
    - creditCeiling doc matches behavior (it guards the small-cap/unbounded case; it does not cap max(cap,1000) downward) — comment fix only, no behavior change.
  </behavior>
  <action>
Close M-2, M-3 and the MQ MINORs. Read REVIEW-FINDINGS for exact line refs. Every
change must keep `go test -race ./internal/server/...` green — write/adjust tests
alongside.

M-2 (server.go Consume, the store-empty park ~line 260-275): capture
`ch := s.notifyChan()` BEFORE calling `s.store.TryDequeue()`; only if the dequeue
misses, park on that captured `ch`. This closes the missed-wakeup window where a
Produce between the failed dequeue and the channel fetch swaps in a fresh channel and
the consumer waits on the wrong one. TDD: single consumer, produce exactly one
message AFTER the consumer is confirmed parked, assert the message is delivered.

M-3 (server.go Consume + cmd/mq/main.go): GracefulStop does not cancel server-side
stream contexts and Consume streams are infinite, so SIGTERM hangs until SIGKILL and
httpSrv.Shutdown is never reached; shutdownCh is currently write-only dead code. Add
`case <-s.shutdownCh:` returning `status.Error(codes.Unavailable, "shutting down")`
to the send loop's blocking selects (the existing defer requeues leases). Fix the
misleading comment at server.go:322-324 about grpc-go GracefulStop semantics. In
cmd/mq/main.go, race GracefulStop against a bounded timeout and fall back to
grpcSrv.Stop() so shutdown always completes. TDD: start a Consume stream, trigger
shutdown, assert Consume returns and shutdown completes within a bound.

MQ MINOR (disconnect cleanup, server.go ~189-210): reorder cleanup to drain the ackCh
and apply those acks FIRST, then requeue only the still-unacked survivors; delete the
now-dead `if _, ok := leases[id]` branch.

MQ MINOR (recv terminal error, server.go ~232-233): translate a clean client
CloseSend — if `errors.Is(recvFinalErr, io.EOF) { return nil }` — so the client sees
a clean close instead of codes.Unknown.

MQ MINOR (creditCeiling doc, server.go ~37-40, ~296-305): correct the comment to say
the ceiling only guards the small-cap / unbounded-backend case; do NOT change
behavior (still returns max(cap, creditCeiling)).

Do not break documented decisions: drop-oldest ring, bidi credit/ack protocol
(ADR-001), the two-goroutine bidi split (recv sole Recv caller, send loop sole Send
caller).
  </action>
  <verify>
    <automated>go test -race ./internal/server/... ./cmd/mq/...</automated>
  </verify>
  <done>M-2, M-3 and the MQ MINORs are closed; missed-wakeup and shutdown-under-active-stream tests fail before the fix and pass after; -race stays green; ADR-001 delivery semantics preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Gateway/docs/build — truncation header (M-4), collision ADR (M-5), honest lint gate (G-2), Docker-env docs (G-3), AI-usage deliverable (G-4), gateway/db minors, swagger regen</name>
  <files>internal/gateway/handler.go, internal/gateway/integration_test.go, pkg/db/read.go, Makefile, README.md, docs/adr/ADR-002-natural-key-microsecond-collision.md, docs/AI_USAGE.md, .planning/REQUIREMENTS.md, pkg/docs</files>
  <action>
Close M-4, M-5, G-2, G-3, G-4 and the gateway/db/test MINORs. Read REVIEW-FINDINGS
for exact line refs and the OUT-OF-SCOPE boundaries (M-4 is header-signal only — NO
pagination; M-5 is document-only — NO schema change).

M-4 (internal/gateway/handler.go GetTelemetry): when `len(metrics) == maxRows`, set
response header `X-Truncated: true` and `X-Row-Limit: <maxRows>` before writing the
body (keep the 200 + DESC-ordered array). Add swag `@Header 200 {string} X-Truncated`
annotations documenting the signal; keep DESC ordering (documented decision — do not
flip). Regenerate the OpenAPI spec via `make swagger` (never hand-edit pkg/docs).
Add one README sentence in the Phase 4 / API section justifying newest-first (DESC)
ordering and describing the truncation header.

M-5 (docs/adr/ADR-002-natural-key-microsecond-collision.md — next number after
ADR-001): write a short ADR. Streamer stamps ns but TIMESTAMPTZ stores µs, so the
effective natural key is (gpu_id, metric_name, µs); two streamers restamping the same
(gpu,metric) in the same µs collapse to one row under ON CONFLICT DO NOTHING. Record
the collision window, why it is accepted (idempotency for redelivery outweighs the
statistically rare cross-producer µs collision), and the fix path (producer-id in the
key). Reference it from the README design-considerations / exactly-once section.

G-2 (Makefile lint target): branch on binary presence, not exit code, so lint
FINDINGS fail the build — use a command-presence check that runs golangci-lint when
present and falls back to go vet only when the binary is absent. The prior
`golangci-lint run ./... 2>/dev/null || go vet ./...` silently swallowed findings.

G-3 (Makefile + README): document the coverage/e2e Docker requirements
(DOCKER_HOST=unix://$HOME/.rd/docker.sock, TESTCONTAINERS_RYUK_DISABLED=true) as a
comment block near the top of the Makefile and in the README testing section; fix the
README testing-scope drift so it says internal + pkg packages (not "internal/ packages"
only).

G-4 (assignment deliverable — AI-usage docs): (a) add requirement DOC-02 to
.planning/REQUIREMENTS.md under the DOC section AND to the traceability table so later
phases keep it alive. (b) create docs/AI_USAGE.md documenting the real workflow
honestly: built with Claude Code + the GSD framework, per-phase
discuss/plan/execute/verify with planning artifacts under .planning/phases/* (the
plans ARE the prompts), agent role briefs in .ai/agents/, ADRs for design decisions;
include a per-phase table for phases 1–4 linking to the plan files, and a
"where AI fell short / manual interventions" section seeded from STATE.md decisions
(Phase 01.1 silent-loss reproduction that triggered ADR-001, swag runtime version
mismatch, Rancher Docker socket env). (c) add a README "AI-Assisted Development"
section summarizing and linking to docs/AI_USAGE.md.

Gateway MINOR (pkg/db/read.go 65-92): delete the verbatim-duplicated godoc block on
the Telemetry function (keep one copy).

Gateway MINOR (per-request DB deadline): wrap the handler DB calls with a ~10s
deadline — chi middleware.Timeout on the telemetry route or context.WithTimeout in
the handler around the GPUExists/Telemetry calls.

Test MINOR (internal/gateway/integration_test.go): remove the leftover diagnostic
fmt.Printf around line 167; strengthen the near-vacuous window assertion around
lines 266-267 to assert the first returned row IS the newest in-window row (DESC).
  </action>
  <verify>
    <automated>make swagger && make lint && DOCKER_HOST=unix://$HOME/.rd/docker.sock TESTCONTAINERS_RYUK_DISABLED=true go test -race -tags=integration ./internal/gateway/... ./pkg/db/...</automated>
  </verify>
  <done>M-4, M-5, G-2, G-3, G-4 and the gateway/db/test MINORs are closed; X-Truncated header emitted and documented in regenerated pkg/docs; ADR-002 exists and is referenced from README; make lint fails on findings; docs/AI_USAGE.md + DOC-02 exist and are traceable; gateway integration tests pass with the strengthened window assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Streamer→MQ (gRPC) | untrusted proto payloads cross into the broker |
| MQ→Collector→Postgres | at-least-once delivery + idempotent persist boundary |
| client→Gateway (HTTP) | untrusted query params (id, start_time, end_time) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-ku8-01 | Tampering/Repudiation | Collector persistBatch/flush | critical | mitigate | C-1: return error on failed insert and do NOT ack — broker requeue-on-disconnect + ON CONFLICT preserve no-loss/no-dup (Task 1) |
| T-ku8-02 | Denial of Service | MQ Consume shutdown | high | mitigate | M-3: shutdownCh in send-loop selects + GracefulStop timeout→Stop() so SIGTERM cannot hang the broker (Task 2) |
| T-ku8-03 | Denial of Service | MQ notify/park | medium | mitigate | M-2: capture notifyChan before dequeue so a burst tail cannot stall a parked consumer indefinitely (Task 2) |
| T-ku8-04 | Information Disclosure | Collector/DB error strings | low | accept | Existing convention keeps DSN out of errors; new C-1/db errors follow it — verified by review, no code change beyond wrapping |
| T-ku8-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new package installs in this task — all deps already pinned in go.mod; legitimacy gate not applicable |
</threat_model>

<verification>
Run the full gate suite at the end (this is the REVIEW-FINDINGS "Constraints" bar):

```
make build && make test && make coverage && make lint
```

Integration + e2e tests require the Rancher Docker env:
`DOCKER_HOST=unix://$HOME/.rd/docker.sock TESTCONTAINERS_RYUK_DISABLED=true`.
MQ changes additionally need `go test -race ./internal/server/...` green.

Then cross-check every finding ID in `260702-ku8-REVIEW-FINDINGS.md`
(C-1; M-1..M-6; G-1..G-4; each MINOR) is addressed or explicitly left as
OUT-OF-SCOPE (Dockerfiles/Helm/kind, /healthz//readyz, schema change,
pagination — all deferred to Phase 5/6).
</verification>

<success_criteria>
- C-1 closed: failed DB insert no longer acks; new test proves zero acks + error propagation.
- All MAJOR (M-1..M-6) and MEDIUM (G-1..G-4) findings closed; cheap MINORs closed.
- Every listed test is TDD'd (written to fail first, then pass).
- docs/adr/ADR-002 and docs/AI_USAGE.md exist; DOC-02 added and traceable.
- `make build && make test && make coverage && make lint` all pass; coverage ≥ 90%; `-race` green.
- No documented decision broken (drop-oldest ring, DESC ordering, ON CONFLICT DO NOTHING, ADR-001 bidi protocol, swag-generated OpenAPI).
- Conventional Commits, atomic per logical unit, no Co-Authored-By trailer.
</success_criteria>

<output>
Create `.planning/quick/260702-ku8-fix-all-the-gaps-identified-as-part-of-t/260702-ku8-SUMMARY.md` when done, listing each finding ID and its resolution (or OUT-OF-SCOPE).
</output>
