---
phase: quick-260725-0c4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/gateway.readme.md
  - docs/FUTURE.md
  - docs/streamer.readme.md
  - docs/development.readme.md
  - docs/adr/ADR-010-pagination.md
  - deployments/monitoring/README.md
  - pkg/db/migrations/000002_dlq_table.up.sql
  - CLAUDE.md
  - .claude/CLAUDE.md
  - api/proto/mq.proto
  - pkg/pb/mq.pb.go
  - pkg/pb/mq_grpc.pb.go
  - internal/streamer/config.go
autonomous: true
requirements: []

must_haves:
  truths:
    - README describes the current shipped surface (metrics, DLQ endpoints, backlog HPA, keyset pagination, partitioned retention) and full setup path
    - No doc, ADR, or code comment contradicts the code shipped by PRs #13 and #14
    - Newly-found operational gaps are recorded honestly in docs/FUTURE.md as out-of-scope future work
    - Generated protobuf comment regen is comment-only; build/test/lint gates pass
  artifacts:
    - README.md
    - docs/FUTURE.md
    - deployments/monitoring/README.md
    - api/proto/mq.proto
  key_links:
    - proto comment edit + make proto → pkg/pb comment-only diff
    - monitoring README scrape-config → mq_queue_depth → mq_queue_backlog → Collector HPA
---

<objective>
Fix all documentation, ADR, and code-comment drift left by PR #13 (delivery-semantics
hardening: backpressure, retry/DLQ lanes, lease TTL, poison isolation, preStop drain; ADR-011)
and PR #14 (Prometheus /metrics, backlog HPA, batch publish, keyset pagination, partitioned
retention; ADR-012/013/014). Rewrite the main README as the definitive "latest state + how to
set up" document. Record newly-discovered operational gaps as honest post-v1 future work.

Purpose: The repo must speak a single truth. Docs currently contradict shipped code in 12
identified places (verified against HEAD 64e07b2).

Output: Refreshed README, corrected docs/*.readme.md + ADR-010 + monitoring README + FUTURE.md,
corrected CLAUDE.md files, honest proto/migration/config comments, and a comment-only pkg/pb
regen — all passing build/test/lint gates.

Scope discipline: DOCS AND COMMENTS ONLY. NO behavior changes. The ONLY generated-code change
allowed is `make proto` comment regeneration. Do NOT plan code fixes for the FUTURE.md gaps —
they are explicitly out of scope this iteration. Never edit instructions.md (frozen brief).
Never touch or commit review/ or GAPS-AND-IMPROVEMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md as the definitive current-state + setup doc</name>
  <files>README.md</files>
  <action>
Rewrite README.md so it reflects the shipped state at HEAD (64e07b2) and serves as the
"latest and greatest state + how to set up this solution" document. The Makefile stack-up
banner (Makefile:229-233) already lists /metrics and /queue/dlq — README is behind it; use the
banner as the source of truth for endpoints.

Fix the specific stale claim: README.md:109-111 currently says "Pagination (cursor-free
offset...)" and shows only ?limit/?offset examples. The code has keyset ?cursor= pagination
(internal/gateway/handler.go:186-201,247-271) that emits next_cursor in BOTH offset and cursor
modes. Rewrite this to document keyset cursor pagination (?cursor=) alongside limit/offset, and
show next_cursor in the response envelope.

Additionally refresh the WHOLE README so it covers, at minimum:
- Prerequisites (Go 1.26.x, Docker/Rancher Desktop, kind, Helm v3; DOCKER_HOST + Ryuk notes live in .env.example).
- Make targets (help, tools, proto, build, test, coverage, swagger, lint, docker, kind-up, helm-install/deploy, kind-down).
- docker-compose dev stack pointer.
- kind/helm deploy: `make deploy CSV=<path>` is MANDATORY — no silent fixture fallback (per PR #11 / commit f65d6eb). State that explicitly.
- Try-it sections must include the surfaces currently MISSING: GET /metrics (all services), GET /api/v1/queue/dlq, POST /api/v1/queue/dlq/replay (in addition to existing /api/v1/queue/inspect and gateway read endpoints).
- Scaling section must include the Collector backlog-driven HPA (currently missing) alongside the Gateway CPU HPA.
- Deploy/operations section must include partitioned retention (CronJob + retention.* Helm values).
- Monitoring stack pointer → deployments/monitoring/README.md.
- Links into docs/*.readme.md (mq, streamer, collector, gateway, development) and docs/adr/.

Keep prose tight and accurate; do not invent endpoints or flags not present in code. Cross-check
every endpoint against Makefile:229-233 and cmd/mq/main.go:68-73 before writing it.
  </action>
  <verify>
    <automated>grep -q 'cursor' README.md && grep -q '/metrics' README.md && grep -q '/api/v1/queue/dlq' README.md && grep -q 'CSV=' README.md && ! grep -q 'cursor-free' README.md</automated>
  </verify>
  <done>README documents keyset cursor pagination, /metrics, DLQ endpoints, backlog HPA, partitioned retention, and mandatory `make deploy CSV=<path>`. The stale "cursor-free" phrasing is gone. No fabricated endpoints/flags.</done>
</task>

<task type="auto">
  <name>Task 2: Correct docs/*.readme.md, ADR-010, monitoring README, migration comment, and FUTURE.md</name>
  <files>docs/gateway.readme.md, docs/streamer.readme.md, docs/development.readme.md, docs/adr/ADR-010-pagination.md, deployments/monitoring/README.md, pkg/db/migrations/000002_dlq_table.up.sql, docs/FUTURE.md</files>
  <action>
Apply these exact edits (file:line refs verified at HEAD 64e07b2). Docs/comments only.

(2) docs/gateway.readme.md:57 — change "cursor-free offset pagination" to reflect that keyset
cursor mode exists (lines 154-161 of the same file already document cursor mode; line 57
contradicts them). Also update the response-envelope example (lines 66-76) to INCLUDE the
next_cursor field, which it currently omits. NOTE: the gateway env table in this file is
VERIFIED UP TO DATE — do not touch it.

(3) docs/FUTURE.md:110-111 — currently says Prometheus /metrics export is "(also future work)".
It SHIPPED in PR #14 (every service exposes /metrics; backlog HPA consumes them). Remove the
"future work" framing for /metrics export. The adaptive-sampling idea itself stays as future
work — keep that; only correct the /metrics-shipped claim.

(7-doc) docs/streamer.readme.md — env table (lines 39-44) OMITS STREAMER_BATCH_SIZE
(internal/streamer/config.go:54, default 100; value 1 = legacy per-row). Add a row for it.
Also line 6 describes publishing via "Produce client" — the primary path is now ProduceBatch;
update the description. (The config.go code-comment fix is handled in Task 3.)

(8) docs/development.readme.md — Phase 6 section (lines ~289-302) documents ONLY the Gateway CPU
HPA. Add short subsections for: per-service /metrics endpoints; the Collector backlog-driven HPA
(mq_queue_backlog external metric; deployments/charts/collector/templates/hpa.yaml); and the
partitioned-retention CronJob (deployments/templates/retention-cronjob.yaml, Helm retention.*
values). Cross-link deployments/monitoring/README.md, ADR-012, and ADR-014.

(9) docs/adr/ADR-010-pagination.md:121 — the rejected-alternatives row still argues keyset
pagination is unneeded; it is superseded by the 2026-07-24 amendment at lines 127-138 of the same
file. Add a brief "(superseded — see amendment below)" marker to that row so it can't be read
stale in isolation. Do not delete the row.

(10) pkg/db/migrations/000002_dlq_table.up.sql — the header comment claims poison rows "can be
inspected and replayed"; no API exists (no gateway handler touches gpu_metrics_dlq). Reword
honestly: inspectable via SQL; API surface + retention for this table are future work (see
docs/FUTURE.md). Comment-only edit to an applied migration is safe (golang-migrate does not
checksum file content).

(11) deployments/monitoring/README.md — the setup steps do NOT actually get metrics into
kube-prometheus-stack: metrics are exposed only via prometheus.io/scrape POD ANNOTATIONS, which
kube-prometheus-stack ignores by default (it discovers ServiceMonitor/PodMonitor CRDs; none are
shipped). Add the missing wiring step (DOC-ONLY): a values snippet for kube-prometheus-stack
`prometheus.prometheusSpec.additionalScrapeConfigs` implementing an annotation-based
kubernetes_sd scrape (role: pod, relabel on prometheus.io/scrape|path|port annotations) so that
following the README actually populates mq_queue_depth → mq_queue_backlog → Collector HPA. Note
that shipping a PodMonitor template is future work.

(12) docs/FUTURE.md — add a short "Known operational gaps (tracked, post-v1)" section recording
these newly-found gaps as honest future work (terse, 2-3 lines each; do NOT plan code fixes —
explicitly out of scope):
  (a) Collector poison DLQ (gpu_metrics_dlq) has no API inspect/replay, no alert, not partitioned/no retention.
  (b) Broker DLQ replay is all-or-nothing, resets attempts, unauthenticated.
  (c) STREAMER_BATCH_SIZE has no client-side clamp vs server maxBatchSize=1000, and InvalidArgument is retried as transient.
  (d) Retention ensure_partitions can wedge on the DEFAULT partition after missed runs; CronJob failures have no alert.
  (e) MQ lease sweep interval hardcoded 100ms (no env/values knob).
  (f) StoreStats.DLQEvictedTotal not surfaced in /metrics or /inspect.
  (g) No ServiceMonitor/PodMonitor shipped.

Do NOT touch the VERIFIED-UP-TO-DATE files: docs/mq.readme.md, docs/collector.readme.md,
deployments/values.yaml, the collector hpa.yaml, retention-cronjob.yaml, pkg/docs swagger files,
docs/adr/README.md, docs/DEVIATIONS.md, instructions.md, or the gateway.readme.md env table.
  </action>
  <verify>
    <automated>grep -q 'STREAMER_BATCH_SIZE' docs/streamer.readme.md && grep -q 'next_cursor' docs/gateway.readme.md && grep -q 'superseded' docs/adr/ADR-010-pagination.md && grep -q 'additionalScrapeConfigs' deployments/monitoring/README.md && grep -qi 'operational gaps' docs/FUTURE.md && ! grep -q 'cursor-free offset pagination' docs/gateway.readme.md</automated>
  </verify>
  <done>gateway.readme.md line 57 + envelope corrected (next_cursor present, no "cursor-free" claim); FUTURE.md /metrics claim fixed and gaps section added; streamer.readme.md has STREAMER_BATCH_SIZE + ProduceBatch; development.readme.md documents metrics/backlog-HPA/retention; ADR-010 row marked superseded; migration comment honest; monitoring README has working scrape-config wiring. No verified-up-to-date file touched.</done>
</task>

<task type="auto">
  <name>Task 3: Correct CLAUDE.md files + proto/config comments, regen pkg/pb, run gates</name>
  <files>CLAUDE.md, .claude/CLAUDE.md, api/proto/mq.proto, internal/streamer/config.go, pkg/pb/mq.pb.go, pkg/pb/mq_grpc.pb.go</files>
  <action>
Apply these exact edits (file:line refs verified at HEAD 64e07b2). Comments/docs only, except
the `make proto` regen (comment-only).

(4) api/proto/mq.proto:66-73 — ProduceResponse.accepted comment says ResourceExhausted
backpressure is "reserved for future". PR #13 SHIPPED it (internal/server/server.go:154-169,
reject/block overflow policies). Update the comment: backpressure IS implemented via
ResourceExhausted under reject/block policy; `accepted` stays true on the success path. After
editing the proto comment, run `make proto` so the generated pkg/pb comments match. Then run
`git diff pkg/pb` and confirm ONLY comment lines changed — if any non-comment (struct/field/
wire) line changes, STOP and revert; the regen must be comment-only.

(5) CLAUDE.md — two edits:
  - Line 102 "MQ endpoints" line omits: gRPC ProduceBatch, and HTTP GET /api/v1/queue/dlq,
    POST /api/v1/queue/dlq/replay, GET /metrics, /healthz, /readyz (all live in
    cmd/mq/main.go:68-73). Add them to the MQ endpoints line.
  - Service table (~line 18): Streamer row says it publishes via "gRPC client → MQ Produce".
    The primary path is now ProduceBatch (internal/streamer/streamer.go:162,215). Update to
    reflect ProduceBatch as primary (Produce remains the legacy per-row path).

(6) .claude/CLAUDE.md — stack section drift. It recommends pgx CopyFrom as the persistence path
("Use pool.CopyFrom(...)", alternatives-table row "PostgreSQL bulk insert | pgxpool.CopyFrom",
"COPY is transactional by nature") but the SHIPPED code uses pgxpool SendBatch + ON CONFLICT DO
NOTHING per ADR-007. Update these to record SendBatch as the chosen path, pointing at
docs/adr/ADR-007 (same treatment as the earlier D6 buf→protoc fix — record the reversal, don't
pretend it was always SendBatch). Also its proto pattern bullet says "Produce unary only" — add
ProduceBatch.

(7-code) internal/streamer/config.go:41-46 — the FromEnv doc-comment lists env vars but OMITS
STREAMER_BATCH_SIZE (the field is at config.go:54). Add STREAMER_BATCH_SIZE to that comment list.
Comment-only edit — do not change any code.

Then run the gates (skip `make coverage` — Docker-gated integration tests; this is a docs-only
change):
  make build && make test && make lint
Fix any failure autonomously (a docs/comment change should not break these; if it does, the
regen likely touched non-comment lines — investigate). Confirm `git diff --stat pkg/pb` shows
only comment churn.
  </action>
  <verify>
    <automated>git diff --quiet -- pkg/pb || git diff -U0 -- pkg/pb | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vqE '^[+-]\s*//'; echo "proto-diff-comment-only:$?"; grep -q 'ProduceBatch' CLAUDE.md && grep -q 'SendBatch' .claude/CLAUDE.md && grep -q 'STREAMER_BATCH_SIZE' internal/streamer/config.go</automated>
  </verify>
  <done>proto comment reflects shipped backpressure; pkg/pb regen is comment-only (verified via git diff); CLAUDE.md MQ endpoints + Streamer row updated (ProduceBatch, DLQ, metrics, probes); .claude/CLAUDE.md records SendBatch/ADR-007 + ProduceBatch; config.go FromEnv comment includes STREAMER_BATCH_SIZE; make build/test/lint pass.</done>
</task>

</tasks>

<verification>
- Every stale claim (audit items 1-12) is corrected; no doc/ADR/comment contradicts HEAD 64e07b2 code.
- `git diff pkg/pb` is comment-only (no wire/struct/field changes).
- `make build && make test && make lint` pass (coverage skipped — docs-only, Docker-gated).
- No edit to instructions.md, review/, or GAPS-AND-IMPROVEMENTS.md. GAPS-AND-IMPROVEMENTS.md is never committed.
- Verified-up-to-date files untouched.
</verification>

<success_criteria>
- All 12 audit drift items resolved with the exact edits specified.
- README is the definitive current-state + setup doc (pagination, metrics, DLQ, backlog HPA, retention, mandatory CSV).
- docs/FUTURE.md records the 7 operational gaps as honest, out-of-scope-this-iteration future work.
- pkg/pb regen is provably comment-only; build/test/lint gates green.
- Three atomic Conventional Commits (one per task), NO Co-Authored-By trailer, on branch docs/post-v1-consistency-pass.
</success_criteria>

<output>
Create `.planning/quick/260725-0c4-docs-consistency-pass/260725-0c4-SUMMARY.md` when done.
</output>
