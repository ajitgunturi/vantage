---
phase: quick-260725-0c4
plan: 01
subsystem: docs
tags: [docs, consistency, readme, adr, monitoring]
requires: []
provides:
  - README as definitive current-state + setup doc (keyset pagination, /metrics, DLQ, backlog HPA, retention, mandatory CSV)
  - docs/FUTURE.md "Known operational gaps" register (7 items)
  - Corrected CLAUDE.md / .claude/CLAUDE.md (ProduceBatch, DLQ+metrics endpoints, SendBatch/ADR-007)
affects: [docs, README.md, CLAUDE.md, api/proto, pkg/pb]
tech-stack:
  added: []
  patterns: []
key-files:
  created: []
  modified:
    - README.md
    - docs/gateway.readme.md
    - docs/streamer.readme.md
    - docs/development.readme.md
    - docs/FUTURE.md
    - docs/adr/ADR-010-telemetrypage-pagination-envelope.md
    - deployments/monitoring/README.md
    - pkg/db/migrations/000002_dlq_table.up.sql
    - CLAUDE.md
    - .claude/CLAUDE.md
    - api/proto/mq.proto
    - internal/streamer/config.go
    - pkg/pb/mq.pb.go
decisions: []
metrics:
  duration: ~35m
  completed: 2026-07-25
status: complete
---

# Quick Task 260725-0c4: Docs Consistency Pass Summary

**One-liner:** Post-v1 docs truth pass — resolved all 12 drift items left by PRs #13/#14 (README
rewrite with keyset pagination + DLQ + metrics + backlog HPA + retention, ADR-010 superseded
marker, monitoring scrape-config wiring, honest DLQ migration comment, CLAUDE.md ProduceBatch +
SendBatch/ADR-007 corrections, comment-only pkg/pb regen) and recorded 7 operational gaps in
docs/FUTURE.md.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Rewrite README.md as definitive current-state + setup doc | `6de5080` | README.md |
| 2 | Correct docs/*.readme.md, ADR-010, monitoring README, migration comment, FUTURE.md | `b8c5518` | docs/gateway.readme.md, docs/streamer.readme.md, docs/development.readme.md, docs/adr/ADR-010-telemetrypage-pagination-envelope.md, deployments/monitoring/README.md, pkg/db/migrations/000002_dlq_table.up.sql, docs/FUTURE.md |
| 3 | Correct CLAUDE.md files + proto/config comments, regen pkg/pb, run gates | `4be36da` | CLAUDE.md, .claude/CLAUDE.md, api/proto/mq.proto, internal/streamer/config.go, pkg/pb/mq.pb.go |

## What Changed

- **README.md** — full rewrite: keyset `?cursor=` pagination documented alongside limit/offset
  with the `next_cursor` envelope ("cursor-free" claim removed); try-it sections for
  `GET /metrics`, `GET /api/v1/queue/dlq`, `POST /api/v1/queue/dlq/replay`; Collector
  backlog-driven HPA + Gateway CPU HPA in scaling; partitioned-retention section
  (`retention.*` values); mandatory `make deploy CSV=<path>` stated explicitly; monitoring
  README + ADR-010/012/014 linked.
- **docs/gateway.readme.md** — pagination intro now names both modes; envelope example includes
  `next_cursor`. Env table untouched (verified up to date).
- **docs/streamer.readme.md** — `STREAMER_BATCH_SIZE` row added; intro records ProduceBatch as
  primary path.
- **docs/development.readme.md** — Phase 6 gains three subsections: per-service `/metrics`,
  Collector backlog HPA (`mq_queue_backlog`), retention CronJob; cross-links monitoring README,
  ADR-012, ADR-014.
- **docs/adr/ADR-010** — keyset rejected-alternative row marked "(superseded — see amendment
  below)"; row preserved.
- **pkg/db/migrations/000002_dlq_table.up.sql** — header comment now honest: SQL-only inspection,
  API surface + retention are future work (comment-only; golang-migrate does not checksum).
- **deployments/monitoring/README.md** — added the missing `additionalScrapeConfigs`
  annotation-based kubernetes_sd values snippet (kube-prometheus-stack ignores pod annotations
  by default); PodMonitor noted as future work.
- **docs/FUTURE.md** — `/metrics` "(also future work)" claim corrected to shipped (ADR-014);
  new "Known operational gaps (tracked, post-v1)" section with the 7 audit items (collector DLQ
  surface, coarse broker replay, unclamped STREAMER_BATCH_SIZE, retention DEFAULT-partition
  wedge, hardcoded lease sweep, DLQEvictedTotal not exported, no PodMonitor).
- **api/proto/mq.proto + pkg/pb/mq.pb.go** — ProduceResponse comment records shipped
  ResourceExhausted backpressure (reject/block); `make proto` regen verified comment-only
  (`git diff -U0 pkg/pb` shows only `//` lines; only mq.pb.go changed).
- **CLAUDE.md** — MQ endpoints line adds ProduceBatch + DLQ/metrics/probe HTTP endpoints;
  Streamer service-table row records ProduceBatch as primary.
- **.claude/CLAUDE.md** — pgx row, alternatives table, and pattern bullets record SendBatch +
  ON CONFLICT DO NOTHING per ADR-007 as a documented reversal of the CopyFrom recommendation;
  proto pattern bullet adds ProduceBatch (ADR-013).
- **internal/streamer/config.go** — FromEnv doc-comment lists STREAMER_BATCH_SIZE (comment-only).

## Verification

- Task 1 gate: grep checks (cursor, /metrics, dlq, CSV=, no "cursor-free") — PASS
- Task 2 gate: grep checks (STREAMER_BATCH_SIZE, next_cursor, superseded, additionalScrapeConfigs, operational gaps, no "cursor-free offset pagination") — PASS
- Task 3 gate: pkg/pb diff comment-only — PASS; ProduceBatch/SendBatch/STREAMER_BATCH_SIZE greps — PASS
- `make build` — PASS; `make test` (`go test -race` full module) — PASS; `make lint` — 0 issues
- `make coverage` skipped by instruction (Docker-gated integration suite; docs-only change)
- No deletions across the three commits; instructions.md, review/, GAPS-AND-IMPROVEMENTS.md,
  and all verified-up-to-date files untouched

## Deviations from Plan

1. **Execution location** — the dispatched worktree
   (`.claude/worktrees/agent-ab6c52d72ac9260f6`) no longer existed when recovery was approved;
   the main checkout was already on `docs/post-v1-consistency-pass` at the pre-dispatch commit
   `53cd384`, so execution proceeded there (matches the plan's success criterion "on branch
   docs/post-v1-consistency-pass"). No `git reset --hard` was needed.
2. **ADR-010 filename** — plan referenced `docs/adr/ADR-010-pagination.md`; the actual file is
   `docs/adr/ADR-010-telemetrypage-pagination-envelope.md`. Edits applied to the real file.
3. **README ADR links** — linked ADR-014 (not ADR-012) for the backlog HPA rationale, since
   ADR-012 is daily-partitioning-retention and ADR-014 is prometheus-metrics-queue-depth-hpa;
   ADR-012 linked from the retention section instead.

## Self-Check: PASSED

- Commits 6de5080, b8c5518, 4be36da exist on `docs/post-v1-consistency-pass`
- All 13 modified files present on disk with expected content (verify gates green)
